## 버전 0.74.0(2021/03/13)

### 하위 호환성이 없는 변경

- `Fix` 모바일 앱「Swipe」명령어의 이동방향이 지정한 방향과 약간 벗어나는 경우가 있어 해당 문제 수정하였습니다.  

### 전반

- `New` 테스트 결과 화면에서 테스트가 미완료 상태여도 실행하고 있던 순간까지의「실행 로그」를 확인할 수 있게 되었습니다.
- `New` 테스트 편집 영역 양 끝의 불필요한 여백을 줄였습니다.
- `Fix` 환경「클라우드」「외부 클라우드 서비스」에서의 테스트 실행 시「날짜 계산」에서 에러가 발생하는 경우가 있어 해당 문제 수정하였습니다. 
- `Fix` UI의 스캔 및 자동 복원에 실패하는 경우가 있어 해당 문제 수정하였습니다.

### 모바일 앱 테스트

- `New` 「클립보드에 값을 세팅」명령어를 추가하였습니다.
- `New` 「키보드 키 입력」명령어로 텍스트를 지정할 수 있게 되었습니다.
   - 현재 집중되고 있는 요소에 텍스트를 송신할 수 있습니다.
- `New` 클라우드 디바이스의 안전성을 강화하였습니다.
- `Fix` 「앱을 백 클라우드화」및「메인」의「시스템 버튼을 누른다」로 타임 에러가 발생한 경우가 있어 해당 문제 수정하였습니다.

### 브라우저 테스트

- `New` 테스트 편집 화면에서도 환경에「클라우드」를 지정할 수 있게 되었습니다.  
   - 클라우드의 브라우저를 이용하여 테스트의 작성 및 개별 실행이 가능해졌습니다.

## 버전 0.73.0(2021/02/27)

### 하위 호환성이 없는 변경

- `Fix` 모바일 앱「Flick」명령어의 Flick 량의 계산이 올바르지 않은 경우가 있어 해당 문제 수정하였습니다.  

### 전반

- `New` Locator 생성 시에 우선 이용하지 않도록 랜덤 생성 문자열로써 보다 많은 패턴을 판정할 수 있게 되었습니다.
- `New` 테스트 일괄 실행 화면 로딩 시에 이전 테스트에서 마지막으로 선택한 일괄 실행 설정이 초기 표시되도록 하였습니다. 
- `Fix` 각종 화면 UI의 세세한 부분들을 수정하였습니다.

### 모바일 앱 테스트

- `New` 이용하고 있는 Appium 버전을 갱신하였습니다.
   - BrowserStack : 1.20.2로 갱신하였습니다.
   - SauceLabs : iOS는 1.20.1, Android는 1.19.0으로 갱신하였습니다.
- `New` 클라우드 디바이스 1대의 최대 연속 이용 가능 시간을 유료 요금제의 경우에는 3시간부터 10시간까지로 하였습니다.
- `New` 클라우드 디바이스의 안전성을 강화하였습니다.
- `Fix` 「앱을 종료」명령어 실행 후에 경우에 따라서 「메인」에 대응하는「시스템 버튼을 누른다」명령어에서 에러가 발생한 경우가 있어 해당 문제 수정하였습니다.

### 브라우저 테스트

- `New` Local/Session Storage의「값 세트」「클리어」「값 저장」명령어를 추가하였습니다. 
- `Fix` optgroup 요소 아래에 있는 option 요소를「풀 다운 선택」으로 선택할 수 없는 문제를 수정하였습니다.
- `Fix` 경우에 따라서는 편집 화면에서 환경으로써「클라우드」를 선택할 수 있게 되었습니다.
- `Fix` 경우에 따라서는「클라우드」환경의「모바일 에뮬레이션」테스트 실행이 실패하는 경우가 있어 해당 문제 수정하였습니다. 
- `Fix` 화면에서「로컬 PC」환경의 Internet Explorer에 대응하는 테스트가 실행되지 않는 경우가 있어 해당 문제 수정하였습니다.

## 버전 0.72.0(2021/02/13)

### 하위 호환성이 없는 변경

- `Fix` 「클라우드」환경의 브라우저가「Base URL」을 이용할 수 없는 결함을 수정하였습니다.

### 전반

- `New` 테스트 케이스 중에서 이용하고 있는 UI 요소는 테스트 편집화면의 UI 상에서 가능한 다른 요소에 가려지지 않게 하였습니다.
   - 같은 요소를 몇 번이고 사용할 경우에 매번「이 요소를 일시적으로 가리기」조작을 하지 않아도 되어였습니다.
- `New` 「로컬 PC」테스트에서「추가 경로 증명서」를 지정할 수 있게 되었습니다.
   - 「Web API 콜」명령어로만 유효합니다.
   - 브라우저에서 사이트에 접근 시 추가 경로 증명서가 필요할 경우에 브라우저에 경로 증명서를 등록해야 합니다.
- `New` 요소의 id 속성 등이 UUID를 포함할 경우에는 포함하지 않는 부분만큼 우선순위가 높은 Locator로써 이용하게 되었습니다.
- `New` UI 스캔 속도를 개선하였습니다.
- `New` 테스트 편집 화면에서의 행 삭제 및 잘라내기 한 후에 삭제 및 잘라내기 한 행의 바로 위의 행이 아닌 바로 아래의 행이 선택되도록 되었습니다.
- `New` UI 리스트 분할하여 새로운 섹션 작성할 경우에 분할 위치보다 앞이 아닌 뒤에 있는 UI를 기준으로 새로운 섹션을 만들 수 있도록 되었습니다. 
- `New` 각종 화면 UI의 세세한 부분들을 개선하였습니다. 
- `Fix` 테스트 케이스가 1행도 없는 경우에 복사한 다른 테스트 케이스를 붙여 넣을수 결함을 수정하였습니다.
- `Fix` 「Web API 콜」명령어의 URL의 첫머리에서 변수를 이용하지 못하는 결함을 수정하였습니다.
- `Fix` 테스트 실패 시 자동 복원에 실패하는 결함을 수정하였습니다.
- `Fix` MAC 판 Magic Pod Desktop.app의 업데이트 명령어가 작동되지 않는 결함을 수정하였습니다.

### 모바일 앱 테스트

- `Fix` iOS로 「앱 닫기」명령어를 실행한 후에 화면 조작에 에러가 발생하는 문제를 수정하였습니다.
   - 「클라우드」「로컬 PC」테스트에서만 수정되었습니다.
- `Fix` Android의 Webview 요소를 찾을 수 없는 에러가 발생하는 문제를 수정하였습니다.

## 버전 0.71.0(2021/01/30)

### 하위 호환성이 없는 변경

- `New` iOS 모바일 앱의 「accessbility id」Locator는 원래 name, label, value 속성을 이용했었습니다만, name 속성만을 보도록 변경되었습니다.
   - Appium의 사양 변경에 의한 것입니다. 
   - 영향을 받을 가능성이 있는 기존 테스트에 대해서는 문제가 생기지 않도록 가능한 업데이트를 실시를 완료할 예정입니다.

### 전반

- `New` 조건문 (if 문) 명령어도 복사 및 자르기가 가능하게 되었습니다.
   - 단, 조건 문의 시작과 끝이 적합하지 않은 부분은 복사 및 잘라내기 기능을 사용할 수 없습니다.
- `New` BrowserStack 상의 테스트에서「네트워크 로그」의 취득 여부를 지정 가능하게 되었습니다.
   - BrowserStack 모바일 앱 테스트로 취득한「네트워크 로그」의 경우 Default로는 취득할 수 없게 되었기에, 취득할 필요가 있는 경우에는 테스트 일괄 실행 설정으로 지정해야 합니다. 
- `New` 특히 요소 수가 많은 화면에 대응하는 UI 분석 속도를 크게 개선하였습니다.
- `New` 요소의 id 속성 등이 UUID인 경우에는 그런 속성을 사용한 Locator의 후보 내에서의 순위를 낮추었습니다.
- `New` 자동 복원 기능을 개선하였습니다.
- `New` 네트워크 에러 등의 원인으로 테스트 실행 결과가 서버에 저장 실패할 경우에 몇번이고 재시도 하도록 바뀌었습니다.
- `Fix` PC에 Node.js가 설치되어 있으면 로컬 PC에서의 테스트 실행이 정상적으로 작동되지 않는 경우가 있어 해당 문제를 수정하였습니다.

### 모바일 앱 테스트

- `New` 이용하고 있는 Appium 버전을 1.20.2로 하였습니다.
   - 이로 인하여 로컬 PC 테스트에 Xcode12.3/12.4, iOS14.3/14.4를 이용 가능하게 되었습니다.
- `Fix` 모바일 Safari의 화면을 「HTML을 WebView로써 캡처한다」옵션으로 캡처하면 요소의 좌표가 어긋나는 결함을 수정하였습니다. (iOS13.4이후의 버전만 수정)
- `Fix` 「초간 기다림」명령어로 장시간 대기한 후에 섹션 에러가 발생한 적이 있기에 수정하였습니다.

### 브라우저 테스트

- `Fix` 「클릭하여 파일 업로드」명령어가 정상적으로 작동하지 않는 경우가 있었기에 해당 문제 수정하였습니다.

## 버전 0.70.0(2021/01/16)

### 전반

- `New` 「2단계 인증용 패스워드를 생성하여 저장」명령어를 추가하였습니다.
- `New` 브라우저에서 Magic Pod Desktop을 기동하였을 경우에 자동으로 로그인 처리가 가능하도록 되었습니다.
- `New` 자동 복원 가능을 개선하였습니다.
- `New` GitHub 로그인의 초기화면 디자인을 개선하였습니다.
- `Fix` 프로젝트「갱신」권한이 없으면 공유 Step를 복제할 수 없는 결함을 수정하였습니다.
- `Fix` 화면 캡처 사이즈가 크면 AI Locator에 의해 요소 검색에 실패하였던 적이 있기에 해당 문제 수정하였습니다.
- `Fix` 테스트 일괄 실행 화면에서 여러 대의 디바이스/브라우저 패턴 테스트 실행으로 서버에 에러가 발생한 적이 있었기에 수정하였습니다.  
- `Fix` HTML이 Name Space가 붙은 Tag나 속성을 포함하고 있으면 화면 스캔에 실패한 적이 있기에 해당 문제 수정하였습니다.
- `Fix` UI 상에서 전면에 있어야 마땅할 요소가 뒷면에 숨겨져 있는 문제에 대한 개선을 실시하였습니다. 

### 모바일 앱 테스트

- `New` 화면상의 회전 버튼을 이용하여, 테스트 작성 중에 클라우드 디바이스를 가로로 눕힐 수 있게 되었습니다.
   - 가로모드로 고정한 앱 화면의 캡처가 클라우드 디바이스로 취득할 수 없던 문제점도 해결되었습니다.
- `New` 이용하고 있는 Appium 버전을 갱신하였습니다.
   - BrowserStack: 1.19.1으로 갱신하였습니다.
   - SauceLabs: 1.19.0으로 갱신하였습니다.  
- `New` Remote TestKit 클라이언트에서 기동한 Android 디바이스를 이용하여, FLAG_SECURE 한 앱 화면 캡처를 취득 가능하게 되었습니다.
   - 가상 adb를 유효화하여 이미지 전송 모드를「표준 모드」로 변경할 필요가 있습니다.
- `New` 「Swipe」명령어의 매개변수 초기값을「X:50%, Y:50%, 방향:0°」로 하였습니다.
- `Fix` WebView를 HTML로써 스캔하고 있는 화면으로 테스트 실패하였을 경우에 테스트 결과 화면에서 실패 시 UI 트리를 표시할 수 없던 적이 있기에 해당 문제 수정하였습니다.
- `Fix` 「지정 위치 터치」로 대상 요소의 영역 이외의 부분을 터치할 때 에러가 발생한 적이 있기에 해당 문제 수정하였습니다.

### 브라우저 테스트

- `New` 특별한 변수로써「BASE_URL」을 이용 가능하게 되었습니다.
- `New` 「클라우드」와「로컬 PC」(현재 Mac 상에서만)의 Chrome 테스트 작성 및 실행 시 사이트에 접속하기 위한 「클라이언트 증명서」를 지정 가능하게 되었습니다.
- `New` 「클릭하여 파일 업로드」대상에 input 요소 이외를 지정하여도, 부근의 input 요소를 찾아서 적절하게 업로드 처리할 수 있게 되었습니다.  


## 버전 0.69.0(2020/12/26)

### 전반

- `New` 공유 Step의 복제가 가능하도록 되었습니다.
- `New` 각 테스트의 일괄 실행 결과 페이지에서 테스트 결과 Summary를 볼 수 있게 되었습니다.  
- `New` 「지정 위치 클릭」「지정 위치 터치」로「좌표」를 지정할 수 있게 되었습니다.
- `New` 조작 대상 요소의 상태가 올바르지 않고, StaleElementReference 에러가 생겼을 경우에 몇번 재시도할 수 있게 되었습니다.
- `Fix` 포함되는 UI의 수가 많은 UI 섹션 삭제에 실패한 적이 있었기에 해당 부문 수정하였습니다.
- `Fix` UI 분석 결과에 있어서 다른 요소의 상위에 있어야 마땅할 요소가 하위에 숨겨져 있는 적이 있었기에 해당 부분 수정하였습니다.
- `Fix` 「사칙연산」명령어의 값이 틀렸던 적이 있었기에 해당 부문 수정하였습니다. 
- `Fix` 일부 환경에서 데이터 패턴의 CSV 업로드에 실패하는 결함을 수정하였습니다.
- `Fix` GitHub 계정을 사용한 신규 User 등록이 안되었었는데 해당 문제 수정하였습니다.
- `Fix` 테스트 0번째 줄을 실시 중에 테스트를 중지하여도 중지되지 않았기에 해당 문제 수정하였습니다.

### 모바일 앱 테스트

- `New` 이용하는 Appium의 버전이 1.19.1로 바꾸었습니다.
- `Fix` 「클라우드 업로드」앱 업로드에 실패하였던 적이 있었기에 해당 문제 수정하였습니다.
- `Fix` BrowserStack 동작이 바뀜에 따라「클라우드 업로드」앱으로 BrowserStack 테스트가 실행되지 않았기에 해당 문제 수정하였습니다. 

### 브라우저 테스트

- `New` 「클라우드」환경에서 브라우저 테스트를 실행 가능하게 되었습니다. 
   - 현재「데스크톱 Chrome」과「모바일 에뮬레이션」에 대응하고 있습니다.
   - Standard 요금제에서는 동시 2병렬, Enterprise 요금제에서는 6병렬까지 이용 가능합니다.
- `New` 파일 업로드 조작이 가능한「클릭하여 업로드」명령어를 추가하였습니다.
   - 업로드 대상 파일을 Magic Pod 상에 보관 가능합니다.
   - 클라우드, 외부 클라우드 서비스, 로컬 PC, 모든 환경에서 실행 가능합니다.
- `New` 「클라우드」환경에 있어서 테스트 대상 사이트에 접근 시「클라이언트 증명서」를 지정 가능하게 되었습니다.
- `New` Chrome88과 Edge88에 대응하였습니다.
- `Fix` IT Infra 내 요소가 UI 분석으로 검출되지 않았던 경우가 있었기에 수정하였습니다.
- `Fix` IT Infra 내 요소 조작에 실패한 경우가 있었기에 수정하였습니다.

## 버전 0.68.0(2020/12/13)

### 전반

- `New` [SAML에 의한 싱글 사인온]에서 Magic Pod에 로그인 할 수 있습니다.(엔터프라이즈 요금제만 가능)
- `New` 프로젝트 복제시 다국어 데이터패턴도 복제 가능합니다. (유료 플랜만 가능)
- `New` 자동 복구의 각종 정밀도 개선과 오류 수정을 실시하였습니다.
- `Fix` 실패한 테스트 일괄 실행 결과 기입에 실패할 수 있어 수정했습니다.
- `Fix` 스탠다드 요금제에서 결제 되어야 할 금액의 일부가 계약 당월이 아닌 익월 출금되어 해당 내용을 수정했습니다.
- `Fix` 「테스트 일괄 실행」 일정 설정이 정상적이지 않게 활성화 되어 수정했습니다.
- `Fix` 로컬 PC 테스트 일괄 수행시, 같은 조직의 다른 부팅 중인 Magic Pod Desktop에서도 테스트가 수행되도록 설정되어 있어 수정했습니다.
  - 권한 없는 사용자가 테스트를 참조하거나 실행 할 수 있는 등의 보안상의 문제는 없습니다.

### 모바일 앱 테스트

- `New` 모든 무료 요금제 조직에서, 외부 클라우드 서비스를 사용한 테스트 일괄 실행은 불가능합니다.
  - 기존에는 2019년 11월 이전에 작성한 조직이면 무료 요금제로도 외부 클라우드 서비스를 이용한 테스트 일괄 실행이 가능했습니다.
- `Fix` 타임존 지정시 테스트 편집화면에서 Android클라우드 디바이스 부팅에 실패하는 오류를 수정했습니다.

### 브라우저 테스트

- `New` 어떤 요소가 다른 요소로 덮여있어서 클릭등의 명령어에 에러가 발생할 경우 여러번 재시도 하도록 설정했습니다.
- `Fix` 스크롤된 위치에 있는 요소에 대한 「지정 위치 클릭」이 제대로 작동하지 않는 오류를 수정했습니다.

## 버전 0.67.1(2020/12/2)

### 전반

- `Fix` 자동복구가 발생한 후속 테스트 결과의 상태가 올바르게 표시되지 않아 수정했습니다.
- `Fix` 일부 케이스에서 테스트 케이스의 복제 및 순서변경이 되지 않아 수정했습니다.
- `Fix` 일부 케이스에서 Magic Pod Desktop이 작동되지 않아 수정했습니다.

## 버전 0.67.0(2020/11/28)

### 전반

- `New` 자동복구를 실시한 후, 후속 스텝에서 에러가 발생한 테스트 결과화면에서 자동복구 내용을 강제 승인할 수 있게 되었습니다.
- `New` 자동복구의 세세한 부분을 개선했습니다.
- `New` 「pull-down 선택」 명령어가 공백차이를 무시하고 텍스트를 선택할 수 있게 되었습니다.
- `Fix` 「몇 초간 기다림」명령어의 대기 시간이 길면 테스트 실행시, 에러가 발생 하는 경우가 있어 수정했습니다.
- `Fix` 「로컬 PC」 일괄 테스트 실행 설정에 따라서, 「테스트 일괄실행」 버튼을 클릭해도 Magic Pod Desktop이 시작하지 않는 오류를 수정했습니다.

### 모바일 앱 테스트

- `New` 로컬 PC의 iOS 시뮬레이터에서 Xcode12.2、iOS14.2 이용이 가능합니다.
- `Fix` 「다른 앱 시작」 명령어 parameter에 지정된 변수가 변수로서 인식되지 않는 오류를 수정했습니다.

## 버전 0.66.0(2020/11/14)

### 전반

- `New` 테스트 편집화면에서 복수의 UI를 다른 섹션으로 일괄 이동할 수 있습니다.
- `New` magic_pod_config.json의 language에서 Magic Pod Desktop 명령어 실행시 표시 언어를 지정 할 수 있습니다.
- `New` 자동복구의 세세한 부분을 개선했습니다.

### 모바일 앱 테스트

- `New` 로컬 PC의 iOS 시뮬레이터 테스트에서 「iPhone 12」 시리즈등 새로운 디바이스를 지정 할 수 있습니다.
- `Fix` Xcode12를 사용한 로컬PC의 iOS 테스트에서 「-ios class chain」로케이터가 제대로 작동하지 않는 경우가 있어, 이 경우 xpath를 사용하도록 수정했습니다.
- `Fix` WebView를 HTML로 스캔하는 테스트 실패시 UI 트리가 열리지 않는 문제를 개선했습니다.
- `Fix` 일괄 실행 디바이스 패턴 / 브라우저 패턴에 일본어가 포함되어있으면, SauceLabs 모바일 실제 기기 테스트가 에러가 발생하여 수정했습니다.

## 버전0.65.0(2020/11/1)

### 하위호환되지 않는 변경

- cross-batch-run의 Web API가 복수(디바이스/브라우저) 패턴을 요약한 result 값을 반환해줍니다.

### 전반

- `New` 복수(디바이스/브라우저) 패턴의 일괄실행결과의 여러사항을 개선했습니다.
  - 전체 패턴의 결과를 1개의 결과페이지에서 볼 수 있습니다.
  - screenshots의 Web API와 magic-pod-api-client의 get-screenshots 명령어가 전체 패턴의 화면 캡쳐를 다운로드 할 수 있습니다.
  - batch-runs의 Web API가 전체 패턴을 요약한 result 값을 반환해줍니다.
  - 테스트 결과 메일의 서식을 개선했습니다.
  - 이번 수정에의해 테스트 결과의 진척 표기 숫자가 부정확하게 되었으므로, magic-pod-api-client [0.65.0.1] 버전을 이용해주십시오.
- `New` 조직, 프로젝트, 테스트 케이스 목록에 설명 내용이 표시됩니다.
- `New` 조직 표시명, 프로젝트 표시명, 테스트 케이스명, 공유 스텝명에 사용할 수 있는 문자 종류의 제약이 없어졌습니다.
- `New` 자동복구의 각종 정밀도 개선과 오류 수정을 진행했습니다.
- `New` 테스트별 수행결과의 목록페이지의 읽기 속도를 개선했습니다.
- `Fix` OS 사용자 아이디에 일본어가 있으면 Windows판 MagicPodDesktop.exe 설치에 실패하는 오류를 수정했습니다.

### 모바일 앱 테스트

- `New` 로컬 PC의 iOS 시뮬레이터에 Xcode12.1, iOS14.1을 이용할 수 있습니다.
- `New` iOS 클라우드 디바이스의 안정성을 강화하였습니다.
- `New` [driverExecutablePath] 설정에서 로컬 PC의 Android WebView 테스트에 이용할 Chrome Driver 버전을 지정할 수 있습니다.
- `Fix` 로컬 PC테스트에서 화면 캡쳐 취득 버튼을 연속해서 누르면 UI스캔에 실패하는 오류를 수정했습니다.
- `Fix` 사이즈가 큰 어플리케이션은 업로드 실패하는 경우가 많아 개선했습니다.

### 브라우저 테스트

- `New` 「지정 위치 클릭」명령어를 추가했습니다.
- `New` 조직 설정 화면에서 스탠다드 요금제를 신용카드로 신청할 수 있습니다.
- `New` Chrome87과 Edge87를 지원합니다.
- `Fix` 로컬 PC의 Chrome 테스트 실행이 버전 판정 오류로 실패하는 경우가 있어 개선했습니다.


## 버전 0.64.0(2020/10/18)

### 전반

- `New` UI목록을 개선했습니다. (복수 UI의 일괄 삭제, 공란 섹션 추가 등)
- `New` 일괄 실행 결과 테스트 일람 화면을 개선했습니다.
- `New` 자동 복구의 각종 정밀도 개선과 오류 수정을 진행했습니다.
- `New` 일괄 실행 설정의「테스트 케이스 번호」에서, 1*10 과 같은 기법으로 같은 번호의 테스트를 여러번 반복 실행 가능합니다.
- `New` 다음번 릴리즈의 준비로 magic-pod-api-client을 0.63.0.1로 갱신했습니다.
  - magic-pod-api-client에서 복수 디바이스・브라우저 패턴을 사용하여 일괄 실행을 하는 경우, 다음번 릴리즈(10/31)까지 [0.63.0.1]로 갱신해주십시오. 갱신하지 않으면, 반환값이 올바르게 취득되지 않습니다.
- `Fix` 「날짜 계산」명령어가 Windows에서 작동하지 않는 오류를 수정했습니다.
- `Fix` 테스트 일괄 실행 결과 화면 캡쳐의 Zip파일이 Windows 표준 압축풀기 명령어로 압축풀기가 되지 않아 수정했습니다.

### 모바일 앱 테스트

- `New` iOS의 UI요소에 대하여, [UI 트리에서 검색] 방식의 로케이터를 추가 할 수 있습니다.
   - 일반적인 Appium 로케이터에서 찾을 수 없는 요소가, 이 방식으로 진행하면 찾을 수 있을 확률이 높습니다.
- `Fix` 「값 저장」명령어가 iOS에서 제대로 작동되지 않는 오류를 수정했습니다.
- `Fix` 「Web View 요소를 HTML로 스캔」에 체크했을 때, 스캔을 Android에서 실패하는 케이스가 있어 수정했습니다.

### 브라우저 테스트

- `New` 브라우저 테스트 플랜의 현재 내용 확인과 트라이얼 신청이 조직 설정 화면에서 가능합니다.


## 버전 0.63.0(2020/10/4)

### 전반

- `New` [BrowserStack Local]을 사용하여, 개인 환경 서버에서 BrowserStack테스트가 가능합니다.
- `New` 자동 복구의 정밀도를 개선했습니다.
- `Fix` Magic Pod Web 서버가 무거워지는 문제들에 대하여 개선했습니다.
- `Fix` 「사칙연산」명령어에서 0을 지정하면 제대로 작동하지 않는 오류를 수정했습니다.
- `Fix` 공유단계를 포함한 테스트 결과 화면 캡쳐의 다운로드를 실패하는 오류를 수정했습니다.
- `Fix` Web API에서 테스트 실행시에 오류 메세지가 정확하게 뜨지 않는 경우가 있어서 수정했습니다.

### 모바일앱 테스트

- `New` 「클립보드에 값 저장」명령어를 추가했습니다.
  - iOS기기에서는 아직 호환되지 않습니다.
- `New` Android 클라우드 시뮬레이터 버전「11.0」을 지정 할 수 있습니다.
- `New` 클라우드 디바이스와 로컬 PC에서 이용하는 Appium 버전은 [1.18.2]입니다.
  - Xcode12가 제대로 작동하지 않는 문제는 개선 진행 중입니다.
- `Fix` 클라우드 디바이스의 표시와 작동에 대한 세부적인 문제를 수정했습니다.

### 브라우저 테스트

- `Fix` UI스캔이 제대로 작동하지 않는 오류를 수정했습니다.


## 버전 0.62.0(2020/9/23)

### 전반

- `New` UI요소/변수 값의 수치비교결과 확인/대기/조건분기 명령어를 추가했습니다.
- `New` 테스트 실패를 AI로 자동 복구하고 싶을 경우, 테스트 결과 화면에서 AI 개선 요청을 전송할 수 있습니다.
- `New` 테스트 편집 화면을 개선했습니다.(디자인 개선, 테스트 편집 영역 하단의 불필요한 여백 삭제 등)
- `New` Magic Pod Desktop 로그인 실패 시의 메시지를 개선했습니다.
- `New` 테스트 결과 화면과 조직의 멤버 일람표시를 개선했습니다.
- `New` 자동 복구의 정밀도를 개선했습니다.
- `Fix` Magic Pod의 Web 서버가 무거워질 수 있는 문제들에 대해서 개선했습니다.
- `Fix` 데이터량이 많은 조직 및 프로젝트를 삭제하면 오류가 발생하는 오류를 수정했습니다.
- `Fix` UI에서 전면적 요소가 후면에 가려지는 문제에 대해 개선했습니다.
- `Fix` 프로젝트의 「새로 고침」 권한이 없으면 공유 스텝을 삭제할 수 없는 오류를 수정했습니다.

### 모바일앱 테스트

- `New` 클라우드 디바이스를 테스트 편집 화면과 같은 브라우저 창에서 사용할 수 있습니다.
- `New` 클라우드 디바이스의 각종 표시를 개선했습니다.
- `New` 클라우드 디바이스를 외부로 연결할 때 이용하는 IP의 범위를 207.254.20.32/27로 정했습니다.
- `Fix` 복수의 WebView를 포함한 Android 앱의 UI스캔이 제대로 진행되지 않는 문제점을 수정했습니다.
- `Fix` Android의 Chrome이 버전 85인 경우, WebView의 UI 스캔/테스트 실행을 실패하는 문제를 수정하였습니다.
- `Fix` iOS실제기기를 Magic Pod Desktop으로 연결시에 WARNING메시지가 나타나는 문제점을 수정했습니다.

### 브라우저 테스트

- `New` Chrome86과 Edge86를 지원합니다.


## 버전 0.61.0(2020/9/5)

### 하위 호환되지 않는 변경

- `Fix` 대부분 같은 사이즈의 화면이지만, 태스트 결과화면의 캡쳐 사이즈가 매번 변경되는 문제를 해결했습니다.
  - 이로인해, 테스트 결과화면의 캡쳐사이즈가 이전과 달라집니다.

### 전반

- `New` 「텍스트입력」 명령어로, 여러 줄의 텍스트를 입력할 수 있습니다.
- `New` 테스트 결과에 대한 문의사항에 테스트 작성 시 화면 캡쳐와 UI 트리를 첨부할 수 있습니다.
- `New` 프로젝트 복제 시에 테스트 라벨과 일괄 실행 설정이 복제되고, 테스트 복제시에는 테스트 라벨이 복제됩니다.
  - 일괄 실행 설정에서는 공유 설정과 본인 설정만 복제됩니다.
- `Fix` 개행 문자를 포함한 locator가 작동하지 않는 오류를 수정했습니다.
- `Fix` 동시에 다수의 UI스캔이 진행되면 오류가 생길 수 있어 수정했습니다.
- `Fix` 테스트명이 길면 테스트 일람에서 표시가 붕괴되는 문제를 수정했습니다.
- `Fix` 잘못된 xpath locater가 생성될 수 있어 수정했습니다.
- `Fix` Magic Desktop 버전업 확인 다이얼로그에서 다운로드 페이지를 열수 없어서 수정했습니다.

### 모바일앱테스트

- `New` 「화면 중앙을 멀티터치 조작」 명령어를 추가했습니다.
- `New` 「시스템 버튼 누르기」 명령어 paramete로 길게 누를 것인지 선택 가능합니다.
- `New` 파일명에 영숫자와 기호 이외의 문자가 포함된  앱도 테스트 가능합니다.
- `New` 이용하고 있는 Appium의 버전을 갱신하였습니다.
  - 클라우드, 로컬PC, SauceLabs: [1.18.1]로 갱신했습니다.
  - BrowserStack: iOS를 1.18.0으로 업데이트 하였습니다.
- `New` 로컬 PC의 iOS 시뮬레이터로 Xcode11.6, Xcode12(베타), iOS13.7, iOS14.0(베타)을 이용할 수 있습니다.

### 브라우저 테스트

- `Fix` HTML 속성명에 「;」 「?」가 포함되어 있으면 UI스캔 에러가 발생하는 불편을 수정했습니다.

## 버전 0.60.0(2020/8/22)

### 전반

- `New` 테스트케이스에 「라벨」을 설정할 수 있습니다.
- `New` 일괄 실행 시 특정 라벨을 가진 테스트만을 대상으로 진행하거나 특정 라벨을 가진 테스트를 대상에서 제외할 수 있습니다.
- `New` 「정규표현에 일치한 부분을 저장」 명령어를 추가했습니다. 
- `Fix` 플랜 페이지에서 스탠다드 플랜을 신청하면 유효하지 않아 수정했습니다.

### 브라우저 테스트

- `New` Chrome85에서 이용가능합니다.

## 버전0.59.0(2020/8/8)

### 전반

- `New` 사용자의 「메일 설정」에서 테스트 결과에 대한 메일 수취 여부를 설정할 수 있습니다.
- `New` 「텍스트 입력」 명령어의 인수에서 「최대 재시도 횟수」를 지정할 수 있습니다. 텍스트 입력이 불안정한 곳에서 이용할 수 있습니다.
- `New` 테스트 결과 일람 화면에서 결과 수가 많은 경우에는, 2 페이지 이후부터 표시됩니다.
- `New` 테스트 결과 일람 화면의 읽기 속도를 개선했습니다.
- `New` UI 검사 속도를 개선했습니다.

### 모바일앱테스트

- `New` Remotet TestKit 테스트 실행중에, 테스트 결과 화면에서 「실시간 동영상」을 확인할 수 있습니다.

### 브라우저 테스트

- `New` 「단축키 입력」명령어를 추가했습니다.
- `New` 특별 변수로「OS」「DEVICE_TYPE」를 이용할 수 있습니다.
- `New` Windows판 Magic Pod Desktop 설치 시, OS 경고 메시지가 뜨지 않습니다.
- `Fix` 화면 캡쳐 범위「HTML」의 화면 캡쳐가 Windows상 Chrome에서 제대로 캡쳐가 되지 않아 수정했습니다.

## 버전 0.58.0(2020/7/24)

### 하휘호환 되지 않는 변경

- `New` Windows상의 MagicPodDesktop.exe을 사용한 명령어 라인 실행에 OS 프록시 설정을 이용하지 않도록 했습니다.
  - 이 처리가 원인으로 테스트가 영원히 종료되지 않을 수 있습니다.
  - Windows상 MagicPodDesktop.exe 명령어 라인 실행에서 프록시 서버를 이용해야하는 경우, [magic_pod_config.json]에서 직접 지정 해주세요.
- `Fix` iOS앱의「표시될 때까지 스와이프」가 장시간 지속 되거나, 잘못된 방향으로 스와이프 되는 문제를 수정했습니다
  - 이번 업데이트로 인해,「표시될 때까지 스와이프」의 움직임이 바뀌는 경우도 있습니다.

### 전반

- `New` 「일시계산」명령어의 「언어설정」에서 「중국어」선택이 가능합니다.
- `Fix` 「사칙연산」명령어의 parameter 변수를 지정할 수 없는 오류를 수정했습니다.

### 모바일앱 테스트

- `New` 「쉐이크」명령어를 추가했습니다.
  - 「애뮬레이터」「시뮬레이터」에서 이용할 수 있습니다.
- `New` 「길게 누름 & 이동」명령어를 추가했습니다.
- `New` 테스트 대상 지정 패널에서「앱 타임 존」을 지정할 수 있습니다.
- `New` 테스트 대상 지정 패널「디바이스 언어」에서「중국어」를 선택 할 수 있습니다.
- `New` 「WebView 요소를 HTML로 스캔」을 off로 설정하고 스캔한 UI를 on으로 재업로드한 경우의 작동상황을 개선했습니다.
- `New` Remote TestKit을 통한 테스트 일괄 실행 화면 캡처가 FLAG_SECURE이 유효한 안드로이드 앱에서도 가능합니다.
- `New` 로컬PC의 iOS 시뮬레이터에 Xcode11.6、iOS13.6을 이용 할 수 있습니다.
- `New` Remote TestKit의 Android 테스트에서 사용하는 Appium 버전을 [1.17.1]로 설정하였습니다.
- `New` 테스트 타임아웃 에러가 자주 발생하지 않도록 조치하였습니다.
- `New` 지원 대상 버전인 Java8 이외의 것을 사용하고 있는 경우에도 Magic Pod Desktop의 오류가 발생하지 않도록 조치하였습니다.
- `New` App 파일의 클라우드 업로드에서 .zip 파일 내의 .app 파일명이 영숫자 이외에도 업로드 가능하게 되었습니다.
- `Fix` WebView 관련 명령어의 속도가 느려져서 수정했습니다.

### 브라우저 테스트

- `New` 데스크톱 Chrome 모바일 에뮬레이션 기능을 사용하여, 모바일 Web사이트의 테스트 생성・실행이 가능합니다.
- `Fix` 전각 문자를 포함한 속성명이 포함된 Web 페이지의 UI 스캔이 에러가 되는 오류를 수정했습니다.

## 버전 0.57.0(2020/7/11)

### 전반

- `New` 각각의 테스트별로 테스트 결과 일람 화면에 테스트를 실행한 디바이스명・iOS 버전・브라우저 등의 라벨이 표시됩니다.
- `New` 테스트 일괄 실행 결과 일람의 라벨에서 「환경」과「브라우저 버전」의 정보가 표시됩니다.
- `New` 「화면 캡쳐 취득」명령어의 인수에서 화면 캡쳐명을 지정할 수 있습니다.
- `New` 테스트 일괄 실행 결과 취득 Web API에서 parameter min_batch_run_number과 max_batch_run_number, 되돌림값 started_atとfinished_at를 추가했습니다.
- `New` 화면 캡쳐 다운로드의 Web API에서 parameter file_name_body_type、mask_dynamically_changed_area를 추가했습니다.
- `New` [magic-pod-api-client 0.57.0.1] 에서、get-screenshots에 parameter file_index_type、file_name_body_type、download_type、mask_dynamically_changed_area를 추가했습니다.
- `New` 공유 단계의 parameter 관련 표시를 개선했습니다.

### 모바일앱 테스트

- `New` 1회의 스캔으로 복수의 WebView를 찾은 경우에도,「Web표시를 HTML로 스캔」 설정이 올바르게 작동됩니다.
- `New` 「스와이프」명령어 좌표 지정 방법이 절대값 지정에서「%」지정으로 변경되었습니다.
- `Fix` 「다른 앱 부팅(Android)」명령어의 오류를 수정했습니다.

### 브라우저 테스트

- `New` BrowserStack/SauceLabs를 사용한 테스트가 Internet Explorer에서도 가능합니다.

## 버전 0.56.0(2020/6/28)

### 전반

- `New` [사칙연산] 명령어를 추가했습니다.
- `New` SauceLabs/BrowserStack측 테스트 결과 페이지에서 대응하는 Magic Pod 프로젝트명/일괄 실행 번호/테스트명을 확인할 수 있습니다.
- `New` 특별변수로 현재 실행환경을 나타내는 [ENVIRONMENT]를 이용할 수 있습니다. 
- `New` UI해석 속도를 개선했습니다.
- `New` magic_pod_config.json상에서, Magic Pod Desktop가 이용하는 [프록시 정보]를 지정할 수 있습니다.
  - proxyServerUrl、proxyServerAuthType、proxyServerAuthUser、proxyServerAuthPassword가 지정 가능합니다.
- `New` 「일시저장」명령어의 이름을「일시계산」으로 변경하였고,「시스템」탭으로 이동되었습니다.
- `New` 사용자 신규 등록시, 모바일앱테스트 스탠다드플랜 1개월무료 계정을 신청할 수 있습니다.
- `New` 조직의 플랜 페이지에서 테스트 케이스 수 150을 선택할 수 있습니다.
- `Fix` 테스트 실패시의 자동복구 실패 오류를 수정했습니다.
- `Fix` 테스트 대상 지정 패널에서 제어문자등을 입력하면 패널이 열리지 않는 경우를 수정했습니다.
- `Fix` 환경변수 HTTP_PROXY、HTTPS_PROXY、NO_PROXY가 지정되어 있으면 Magic Pod Desktop이 정상적으로 작동하지 않는 오류를 수정했습니다.

### 브라우저 테스트

- `New` [BrowserStack]/[SauceLabs]를 사용한, 화면에서 일괄 테스트 실행/Web API실행/스케쥴 실행이 가능합니다.
- `New` Chromium Edge를 사용한 테스트가 가능합니다.
  - Windows판과 Mac판 양쪽 다 대응하고 있습니다.
- `New` magic_pod_config.json에 [driverExecutableDir]를 설정함으로서, Magic Pod Desktop에서 통상 지원되지 않는 버전의 브라우저에서도 테스트가 가능합니다.

## 버전 0.55.0(2020/6/13)

### 전반

- `New` 유료 요금제 결제에 사용하는 카드를 여러장 등록·변경·삭제가 가능합니다.
- `New` 테스트 결과 화면의 Appium/Selenium 로그를 보기 쉽게 변경했습니다.
- `New` 테스트 결과 화면의 Appium/Selenium 로그를 클립보드에 복사할 수 있습니다.
- `New` 최근 실행했던 테스트 결과를 WebAPI로 취득할 수 있습니다.
- `New` [magic-pod-api-client 버전 0.55.0.1]은 최근 실행했던 일괄 테스트 실행 번호 취득 및 화면 캡쳐 다운로드가 가능합니다.
- `Fix` 테스트 실패시, 자동복원에 실패하는 오류를 수정했습니다.

### 모바일 앱 테스트

- `New` 리뉴얼 된 새로운 Bitrise 스텝「Magic Pod」를 출시했습니다.
  - 테스트 설정 번호에서 Magic Pod의 설정을 참조하거나, WebAPI에 가까운 형식으로 설정 값을 Bitrise 스텝에서 유지할 수 있습니다.
  - 기존 Bitrise 스텝에서는 지원되지 않았던 다양한 테스트 일괄실행 parameter를 지정할 수 있습니다.
  - 테스트 종료 및 성공 시에 앱을 삭제할 수 있습니다.
  - 기존의 「Magic Pod UI test」 단계는 이제부터 권장하지 않습니다.
- `New` iOS 클라우드 시뮬레이터 버전에「11.4」를 추가했습니다.
- `New` 「다른 앱 부팅」명령어를 추가했습니다.
- `New` Android5、6의 클라우드 에뮬레이터에서도 Web View 테스트가 작동합니다.
- `New` 모바일 앱 테스트 무료 플랜에서 이용 가능한 클라우드 디바이스의 첫 달 이용시간이 10시간으로 증가되었습니다.
- `New` SauceLabs에서 사용하는 Appium 버전이 [1.17.1]로 변경되었습니다.

### 브라우저 테스트

- `New` Safari를 사용한 로컬 PC 테스트가 가능합니다.
- `New` Chrome84를 지원합니다.

## 버전 0.54.2(2020/6/7)

### 전반

- `Fix` 조건에 따라서, Magic Pod Desktop을 사용하여 화면 캡쳐를 실패하는 경우를 수정했습니다.

## 버전 0.54.1(2020/6/3)

### 모바일 앱 테스트

- `Fix` 조건에 따라서, Magic Pod Desktop을 사용하여 Android의 화면 캡쳐를 실패하는 경우를 수정했습니다.

## 버전 0.54.0(2020/5/30)

### 전반

- `New` UI 목록 리스트 형식 표시를 사용하기 쉽게 추가했습니다. (한 번에 많은 UI 확인 가능, UI 미리보기 가능, UI명 끝까지 확인 가능 등)
- `New` UI 해석의 세세한 부분을 개선했습니다.
- `Fix` Chrome 버전 83에서 화면 레이아웃이 무너지는 문제를 수정했습니다.

### 모바일 앱 테스트

- `New` 「앱 부팅」옵션에「디바이스 리셋」을 추가했습니다.
  - 「클라우드 디바이스」「SauceLabs」「BrowserStack」에서 이용 할 수 있습니다.
- `New` 로컬 PC의 iOS시뮬레이터 버전으로 13.5 지정이 가능합니다.
- `New` Remote TestKit(온프레미스)에서 사용하는 Appium 버전을 [1.16.0]으로 업그레이드 했습니다.
- `Fix` BrowserStack에서 Android의 Appium 로그가 취득 불가능한 오류를 수정했습니다.
- `Fix` 머신 부하가 높은 경우 iOS 클라우드 디바이스에서 Web View 테스트가 실패하는 오류를 수정했습니다.
- `Fix` Android에서「picker값 입력」이 작동하지 않아 수정했습니다.
- `Fix` Android 클라우드 디바이스의 동작이 불안정하여 수정했습니다.

### 브라우저 테스트

- `New` URL이「일치하는지」「일치하지 않는지」「포함하는지」「포함하지 않는지」의「확인」명령어를 추가했습니다.
- `New` 제목이「포함되는지」「포함되지않는지」의「확인」명령어를 추가했습니다.
- `Fix` 「마우스 오버」의 명령어가 작동하지 않는 경우가 있어 수정했습니다.

## 버전 0.53.0(2020/5/17)

### 하위 호환되지 않는 변경 (모바일 앱 테스트)

- `Fix` 「앱 부팅」 옵션으로 「프로세스 재기동」을 지정한 경우, iOS 클라우드 시뮬레이터에서 앱이 재설치되는 오류를 수정했습니다.

### 전반

- `New` 테스트 편집화면에서「코멘트」와 「글을 쓰지 않고 비워둔 줄」을 지정할 수 있습니다.
- `New` Web API의 cross-batch-run과 [magic-pod-api-client]의 batch-run에서, 테스트 설정의 번호를 지정하고 테스트를 실행 할 수 있습니다.
  - Web API에서 실행할 설정을 화면 측에서 관리할 수 있습니다.
  - test settings number와 test settings를 모두 지정하면, 번호 설정을 이용하면서 일부만 변경하여 실행할 수도 있습니다.
  - [magic-pod-api-client] 버전은 [0.53.0.1]를 이용해주세요.
- `New` [도움말 페이지]의 불로오기 속도를 개선했습니다.
- `New` 테스트 결과 화면 표시를 개선했습니다.
- `New` 테스트 편집 화면의 표시를 개선했습니다.
- `Fix` 프로젝트 내에 같은 이름의 UI가 여러개인 경우, 테스트 일괄 실행을 실패하는 경우가 있어서 수정했습니다.
- `Fix` 공유 변수 값에「&」가 포함되면 Magic Pod Desktop의 에러가 발생하는 오류를 수정했습니다.
- `Fix` 테스트 결과에 관한 문의가 실패한 케이스가 있어서 수정했습니다.

### 모바일 앱 테스트

- `New` Android 클라우드 시뮬레이터 버전으로 「9.0」「８.1」「8.0」「7.1」「7.0」「6.0」「5.1」「5.0」을 지정할 수 있습니다.
- `New` Android 클라우드 에뮬레이터로 카메라 앱 및 카메라를 사용한 앱이 작동 가능합니다.
  - 촬영 가능한 것은 에뮬레이터가 표시하고 있는 초기 이미지입니다.
- `New` iOS 클라우드 시뮬레이터의 고속 부팅 디바이스는 「13.4」의 「iPhone8」입니다.
- `New` 이용하고 있는 Appium의 버전이 1.17.1로 업그레이드 되었습니다.
  - iOS13.4에서 HTML로 스캔 한 WebView의 테스트가 작동하지 않는 문제 등을 해결하고 있습니다.
- `New` 로컬 PC의 iOS 시뮬레이터 테스트에서「iPhone SE (2nd generation)」「iPad Pro (12.9-inch) (4th generation)」「iPad Pro (11-inch) (2nd generation)」을 지정할 수 있습니다.
- `New` 「WebView요소를 HTML로 스캔」가 유효한 경우에도 스캔 결과에 AI로케이터가 포함됩니다.
- `New` 「WebView요소를 HTML로 스캔」을 지정한 경우의 UI스캔 속도를 개선했습니다.
- `Fix` 외부 클라우드 서비스의 response가 늦게 타임아웃 에러가 발생하는 경우가 있어 수정했습니다.
- `Fix` Remote TestKit 환경 문제로 발생하는 각종 오류들이 추후 발생하지 않도록 개선했습니다.

### 브라우저 테스트

- `New` 사용자 등록시에「브라우저 테스트 기능」무료 트라이얼을 신청할 수 있습니다.

## 버전 0.52.0(2020/4/26)

### 기능 추가

- iOS 클라우드 디바이스 테스트 작성 시에도 기종을 선택할 수 있습니다. (모바일 앱 테스트)
- iOS 클라우드 디바이스에서 버전 13.4과 12.4를 선택 할 수 있습니다. (모바일 앱 테스트)
- Android 클라우드 디바이스에서 선택 가능한 기종이 증가했습니다. (모바일 앱 테스트)
  - 고속 작동 기종은「Pixel 3」입니다.
- 테스트 결과 화면에서 테스트 결과에 대해 문의할 경우 Appium/Selenium 로그, 실패 시 화면 캡처, 실패 시 UI 트리 첨부 내용을 확인/덮어쓰기/삭제 할 수 있습니다.
- 테스트 결과 화면의 세세한 부분을 개선했습니다.
- 자동복구의 각종 정밀도를 개선했습니다.
- 이용하고 있는 Appium 버전을 갱신했습니다. (모바일 앱 테스트)
  - BrowserStack: Android를 [1.17.0]로 갱신했습니다.
  - RemoteTestKit: Android를 [1.16.0]로 갱신했습니다.
  - SaucewLabs: [1.17.0]로 갱신했습니다.

### 오류 수정

- 브라우저의「돌아가기」버튼을 사용하여 테스트・공유 스텝 편집 화면에 돌아가면, 옛날 테스트 케이스가 표시되는 문제를 수정했습니다.
- 테스트 실행 중에 조직·프로젝트명을 변경하면 테스트가 종료되지 않는 오류를 수정했습니다.
- iOS 특정 화면에서 UI 업로드가 실패되는 오류를 수정했습니다. (모바일 앱 테스트)
- iPhone 전용 iOS앱에 대한 테스트를 iPad상에서 실행하면, 일부 명령어가 정상적으로 작동하지 않는 오류를 수정했습니다.(모바일 앱 테스트)
- SauceLabs상에서 테스트 실행이 작동하지 않는 오류를 수정했습니다. (모바일 앱 테스트)
  - SauceLabs의 오류가 원인입니다.

## 버전 0.51.0(2020/4/12)

### 기능 추가

- 테스트 일괄 실행을 화면에서 스케쥴을 지정하여 실행 가능합니다.
  - 현재 모바일 앱 테스트에서만 이용 가능합니다.
- 「날짜 저장」명령어를 추가했습니다.
  - 현재, ~개월전/후, ~일 전/후 등 다양한 일시를 년, 월, 일, 시간, UNIX초, UNIX밀리초, 날짜 서식 지정등 다양한 형식으로 취득하여 변수에 저장할 수 있습니다.
- 테스트와 프로젝트 복제는 백그라운드에서 실시되며, 완료되면 통지메일을 수신합니다.
  - 복제에 시간이 걸리면, 화면 표시에 서버 에러가 발생하는 오류도 개선하고 있는 중입니다.
- 테스트 실패 시의 locator 자동복구에서 그 요소가 사용된 모든 테스트의 작동을 확인할 수 없는 경우, 주의 메시지가 전달됩니다.
- 자동 복구의 세세한 부분을 개선했습니다.
- [magic-pod-api-client][0.50.0.1] 버전에서 HTTP헤더를 지정하는「--http_headers」옵션이 지정 가능합니다.
- 다국어 데이터 패턴과 데이터 패턴의 CSV 권장 encode는 UTF-8입니다.
- 테스트 실행 후에 그 테스트에 사용한 클라우드 업로드 앱을 삭제해도 테스트 결과 화면에서는 계속해서 앱 정보를 볼 수 있습니다. (모바일 앱 테스트)
- WebView테스트와 관련 된 각종 메세지를 개선했습니다. (모바일 앱 테스트)
- 「표시될 때까지 UI 요소를 스크롤」명령어를 추가했습니다. 페이지 전체가 아닌 특정 스크롤 박스를 스크롤 할 수 있습니다. (브라우저 테스트)

### 오류 수정

- 다국어 데이터 패턴에서 CSV출력 파일을 Excel에서 열면 문자가 깨지는 오류를 수정했습니다.
- 다국어 데이터 패턴 CSV 도입 및 데이터 패턴 CSV 업로드에서 파일의 encode에 따라서는 에러 및 문자가 깨지는 오류를 수정했습니다.
- 테스트 케이스의 복제 실패 사례가 있어서 수정했습니다.
- 테스트 실행 중에 테스트 케이스를 삭제하면 테스트 결과가 기입 되지 않는 오류를 수정했습니다.
- 「화면 전체의 묘화가 끝날 때까지 대기」명령어로 대기 중에 디바이스 및 브라우저의 윈도우 사이즈가 변경되면 에러가 발생하는 오류를 수정했습니다.
- iOS에서 요소가 존재하지만, 그 요소를 찾지 못하고 에러가 발생하는 경우가 있어서 수정했습니다. (모바일 앱 테스트)
- 「텍스트 입력」명령어가 반각 가타카나 (일본어 자판에만 해당) 입력이 불가능한 오류를 수정했습니다. (브라우저 테스트)
- 「브라우저 열기」명령어의 「Cookie 삭제」옵션이 작동하지 않아 수정했습니다. (브라우저 테스트)

## 버전 0.50.0(2020/3/28)

### 하휘 호환 되지 않는 기능 추가

- 다양한 type의 input요소에 「텍스트 입력」으로 올바른 값을 셋팅 할 수 있습니다. (브라우저 테스트)
- 다양한 type의 input요소의 값을 올바르게 얻을 수 있습니다. (브라우저 테스트)

### 기능 추가

- Android 클라우드 에뮬레이터로, Google API 및 Goole 계정에 의존한 앱이 작동합니다.
- Android 클라우드 에뮬레이터로 이용할 수 있는 초기 앱 종류가 증가했습니다.
- 자동 복구 결과 확인 다이어로그에서 복구 전과 후의 UI 트리 내용을 확인할 수 있습니다.
- 자동 복구 관련 세세한 부분을 개선했습니다.
- 테스트 편집 화면에서의 UI 목록 불러오기를 고속화했습니다.
- BrowserStack 테스트 실행 시 Appium로그를 테스트 결과 화면에서 확인 할 수 있습니다.
- 이용하는 Appium 버전을 [1.17.0]으로 업그레이드했습니다.
  - iOS의 WebView에 대한 테스트가 crash하는 오류등을 수정하는 중입니다.
- 로컬 PC의 iOS시뮬레이터에 Xcode11.4, iOS13.4를 이용할 수 있습니다.
- 테스트 결과 화면 캡쳐 화질을 개선했습니다. (브라우저 테스트)
- Firefox에서도 비표시 type이 file의 input요소에 대해서 「텍스트 입력」명령으로 파일 패스를 지정하면파일 업로드가 가능합니다. (브라우저 테스트)

### 오류 수정

- 테스트 결과 화면에서 WebView를 포함한 에러 발생시에 UI트리를 표시하면 에러가 발생하는 경우가 있어 수정했습니다.
- parameter에 UI요소를 갖는 공유 단계를 프로젝트 복제하면 복제처에서 paramete가 사라지는 오류를 수정했습니다.
- 프레임내의 컨텐츠가 다른 도메인으로부터 읽혀진 경우에, UI업로드에 실패하는 오류를 수정했습니다.
- 테스트의 단체 실행을 중단하면, 같은 유저의 일괄 테스트 실행도 중단되는 오류를 수정했습니다.
- 화면에 따라서 UI 업로드에 실패하는 오류를 수정했습니다.
- 테스트 개시 및 실행 중에 서버 에러가 발생하는 것을 수정했습니다.
- 테스트 실행 중에 타임아웃 에러가 발생하는 것을 수정했습니다.
- UI수가 많은 프로젝트의 일괄 테스트 실행 및 복제 시에 서버 에러가 발생하는 것을 수정했습니다.

## 버전 0.49.0(2020/3/14)

### 하휘 호환되지 않는 기능 추가

- 각종「변수」명령어와 「Web API call」명령어에서 변수 값으로 boolean 및 숫자를 세팅하여도 문자열로 취급됩니다.
- 변수(다국어 데이터 패턴, 공유 변수, 데이터 패턴 포함)의 이름이 공백이거나 글자 앞 뒤로 공백을 포함하는 경우에 오류로 처리됩니다.
- type이 file의 input요소 값을 얻으면 셋팅된 파일경로가 되돌아옵니다.(브라우저 테스트)

### 기능 추가

- 테스트 설정 팝업 및 Web API에서 「공유변수」를 지정할 수 있습니다.
  - Web API의「credentials」parameter는 권장하지 않으므로, 「shared_variables」를 이용해주세요.
- 테스트 실행 중에 완료된 스텝까지의 화면 캡처를 확인하고 싶은 경우, 테스트 결과 화면에서 확인할 수 있습니다.
  - 페이지의 새로고침은 자동으로 이루어지지 않습니다.
- 요소를 찾을 수 없는 경우, 자동복구가 iframe과 WebView내의 요소에 대해서도 이루어집니다.
- 테스트 실행 로그에 세팅된 변수의 값이 표시됩니다.
- 「키보드 키 입력」명령어로 숫자를 송신할 수 있습니다.
  - 「테스트 입력」명령어로 숫자를 세팅 할 수 없는 요소에 대해 유효합니다.
- 데이터 구동 테스트 값이 ${...}이고 공유 변수나 다국어 데이터 패턴값을 참조할 수 있습니다.
- app파일을 zip형태로 압축해서 업로드 하는 경우에 zip파일 내의 app파일명이 영숫자 이외의 경우 에러가 발생합니다.
- 일부 케이스에서 UI 해석의 속도를 개선했습니다.
- type이 file의 input 요소에 대해 「텍스트 입력」명령어로 파일 경로를 지정함으로써 파일 업로드가 가능합니다.(브라우저 테스트)
  - input요소가 숨김 요소인 경우에는 현재 Chrome만 지원합니다.

### 오류 수정

- 클라우드 디바이스를 재부팅해도, 재부팅 전의 이미지가 계속 표시되는 오류를 수정했습니다.
- testOnly 플래그가 활성화된 안드로이드 앱의 테스트의 에러가 발생하는 오류를 수정했습니다.
- BrowserStack 테스트 실행을 복수의 디바이스에서 병렬로 실시하면, 도중에 앱에 액세스 할 수 없는 오류를 수정했습니다.
- 요소를 찾을 수 없는 경우의 진행되는 자동 복원이 예기치 못한 에러로 실패하는 경우가 있어 수정했습니다.
- WebView가 후면에 있는 경우에 팝업을 스캔할 수 없는 오류를 수정했습니다.
- 제어 문자가 화면 트리상에 포함되어 있으면 스캔이 잘 되지 않는 오류를 수정했습니다.
- 테스트 케이스를 복제하여 작성한 테스트 케이스의 순서가 화면상에서 변경이 잘 안되는 오류를 수정했습니다.
- 스캔으로 생성된 xpath locater가 부정확한 경우가 있어 수정했습니다.
- UI요소의 이름을 변경해도 해당 UI의 자동복원을 승인하면 변경 전의 이름으로 돌아가는 오류를 수정했습니다.
- 복수 디바이스 패턴의 일괄 실행에서 「1개씩 실행」이 잘 되지 않는 것을 수정했습니다.
- 「스위치 조작」 명령어가 변경하려는 값과 현재의 값이 같은 경우에 에러가 발생하여 수정했습니다.
- 일부 케이스에서 다국어 데이터 패턴, 데이터 패턴 값을 제대로 참조할 수 없게 되어 수정했습니다.

## 버전 0.48.0(2020/2/29)

### 하위 호환되지 않는 기능 추가

- [magic-pod-api-client] 버전 [0.47.0.1]에서, 자동복구가 발생한 경우, 되돌림값 2를 반환합니다.

### 기능 추가

- 테스트 실행시에 요소를 찾을 수 없는 경우에 가능할 때는 요소locator의 자동 복원이 진행됩니다.
  - 현재는 테스트 일괄실행을 했을 경우에 한하여 작동합니다.
  - 테스트 결과 화면에서 복구 내용을 승인하면, locator의 변경 내용이 저장됩니다.
  - [magic-pod-api-client] 버전은 [0.47.0.1]를 이용해주세요.
- 테스트 편집 화면의 우클릭 메뉴에서 행 중간에 명령어를 추가할 수 있습니다.
- 테스트 편집 화면의 요소/UI의 드래그&드롭 및 UI 일람 영역의 사용 편리성을 개선했습니다.
- 테스트 편집 화면에서 추가한 locator가 UI 덮어쓰기 업로드를 한 후에도 계승됩니다.
- 프로젝트 및 테스트의 복제 기능 속도를 개선했습니다.
- BrowserStack의 Android테스트에서 사용하는 Appium버전을 [1.16.0]로 업그레이드 되었습니다.
- 「테스트 케이스 번호」가 Bitrise Step에서도 지정 가능합니다.
  - Bitrise Stepの지정 버전이 0.47.1 이상이여야 합니다.
- 「앱 부팅」옵션에서 「첫 회에만 프로세스 재시작」을 선택할 수 있습니다.
  - BrowserStack이외의 환경에서 이용할 수 있습니다.

### 오류 수정

- iOS실제 디바이스상에서 「WebView요소를 HTML로 스캔」이 작동하지 않는 오류를 수정했습니다.
- 복사된 명령어가 포함된 테스트가 있는 프로젝트를 복제하면, 에러가 발생하는 케이스의 수정이 불충분하여 추가로 수정했습니다.
- 앱 업로드를 동시에 진행하면, 부정확한 데이터가 생성되는 것을 수정했습니다.
- 「프로젝트 작성」의 권한이 없는 유저가 기존에 가능했던 몇 가지의 조작이 불가능해져서 사용할 수 없게되어 오류를 수정했습니다.
- WebView요소를 HTML로 스캔을 하면 에러가 발생하여 수정했습니다.
- Magic Pod Desktop에서 테스트를 실행햇을때, [특별한 변수]를 이용할 수 없는 오류를 수정했습니다.
- 일부 플랜에서 설정되어 있는 프로젝트 수의 상한제한이 제대로 작동하지 않는 오류를 수정했습니다.
- 파일 용량이 큰 앱의 업로드를 실패하여 수정했습니다.
- magic_pod_config.json에서 externalAppium을 true로 하면, iOS 테스트에서 요소를 찾을 수 없어서 수정했습니다.

## 버전 0.47.0(2020/2/15)

### 기능 추가

- 「변수 값」이 「일치하는지」「일치하지 않는지」「포함하는지」「포함하지 않는지」의 「확인」「경우」 명령어를 추가했습니다.
- 테스트 케이스의 복제 기능을 추가했습니다.
- 「고정값 저장」 명령값에 다른 변수를 포함할 수 있습니다.
- [magic-pod-api-client]에 delete-app 명령어를 추가했습니다.
  - [magic-pod-api-client] 버전 [0.46.0.1]을 설치해야합니다.
- 테스트 편집화면에서 UI 요소에 추가한 locator를 바로 사용 할 수 있습니다.
- 1개의 테스트의 타임아웃시간이 테스트의 길이에 따라서 변경됩니다.
- 테스트 편집 화면의 각종 개선을 실시했습니다.
- UI요소 관련의 「확인」「대기」「조건분기」 명령어를 변수 계열의 명령어와 구별하기 위해서 「UI요소가...」와 같이 변경되었습니다.
- 앱부팅 parameter name을 「(프로제스 재부팅)」「(프로세스 재부팅)(앱 정보 클리어)」로 변경했습니다.

### 오류 수정

- 테스트 스텝 단계가 많으면 테스트 결과의 기입, 테스트 결과 표시 및 화면 캡쳐의 다운로드 실패에 대해 오류가 발생하는 경우가 있어 수정했습니다.
- upload-file의 Web API의 file parameter가 없으면 서버 에러가 발생하는 오류를 수정하였습니다.
- Remote TestKit상의 WebView 테스트에서 CSS locator가 작동하지 않는 오류를 수정했습니다.
- 클라우드 디바이스를 종료하고 바로 다음 디바이스를 작동할 경우, 다음 디바이스가 작동하지 않는 오류를 수정했습니다.
- Android WebView의 「더블탭」 명령어가 정상적으로 작동하지 않는 오류를 수정했습니다.
- Web API문서에서 delete-file 명령어의 data parameter 형태가 맞지않아 수정했습니다.
- 테스트 일괄 실행 시간이 실제보다 길게 표시되는 경우가 있어 수정했습니다.
- UI 일람 화면의 세세한 오류를 수정했습니다.
- 프로젝트 복제에 실패하는 것을 수정했습니다.
- 테스트/프로젝트의 삭제 처리가 오랜시간 소요되는 것을 수정했습니다.

## 버전 0.46.0(2020/2/1)

### 기능 추가

- iOS 클라우드 디바이스를 사용하여 일괄 실행을 진행할 시에 기종을 선택할 수 있습니다.
- 디바이스 및 브라우저 정보를 취득하는 특별한 변수로 ${OS}、${VERSION}、${MODEL}、${DEVICE_TYPE}、${DEVICE_REGION}、${DEVICE_LANGUAGE}、${BROWSER}를 이용할 수 있습니다.
- 「assert」「체크」명령어를 좀 더 알기 쉬운 「확인」이라는 단어로 변경했습니다.
- 「WebView요소를 HTML로 스캔」설정이 로컬 PC의 iOS 실제 기계와 외부 클라이언트 서비스에서도 이용 가능합니다.
- Android 클라우드 디바이스에서 「초기 앱」「Android 데모 앱」을 선택할 수 있습니다.
- 무료 요금제에서도 클라우드 디바이스를 작성 2대, 일괄 실행 2대까지 동시에 이용 할 수 있습니다.
- Remote TestKit의 iOS 테스트 결과 화면에서 Appium 로그를 확인 할 수 있습니다.
- 이용하고 있는 Appium 버전을 갱신했습니다.
  - 클라우드/로컬 PC: [1.16.0]로 갱신했습니다.
  - BrowserStack: iOS는 [1.16.0], Android는 [1.15.0]로 갱신했습니다.
  - RemoteTestKit: Android를 [1.15.1]로 갱신했습니다.
- 조직에 엑세스 가능한 접속원IP를 조직의 설정화면에서 지정할 수 있습니다. (엔터프라이즈만 가능)
- 브라우저 테스트의 여러 내용을 개선했습니다.
  - 테스트 편집화면에서 테스트 대상의 윈도우 사이즈를 지정 할 수 있습니다.
  - 테스트 편집화면에서 「화면 캡쳐 범위」를 「윈도우」로 지정한 경우, 외부 디스플레이에서 테스트를 실행하면 화면 캡쳐가 제대로 진행되지 않는 문제를 수정했습니다. (Windows만 수정)

### 오류 수정

- magic_pod_config.json의 부정확한 설정값이 원인으로 Magic Pod Desktop이 서버접속 및 로그인이 불가능한 경우가 있어 수정했습니다.
- 「picker값 입력」 명령어가 종종 에러가 발생하여 수정했습니다.
- 일괄 테스트 실행을 동시에 여러개를 시작하면 에러가 발생하는 경우가 있어 수정했습니다.
- JavaScript 다이얼로그가 표시된 경우, WebView 화면 캡쳐 취득 및 테스트 실행이 장시간 다운되는 오류를 수정했습니다.
- 공유 스텝 화면이 열려있어면, 클라우드 디바이스의 화면 캡쳐가 잘 되지 않는 오류를 수정했습니다.
- 프로젝트 삭제 및 테스트 삭제의 오류가 발생하여 수정했습니다.
- 「외부 클라우드 서비스」테스트에서 「로그레벨」설정이 제대로 작동하지 않아 수정했습니다.
- 테스트 일괄 실행 설정의 「테스트 결과를 메일로 통지」체크를 해제하여도 메일로 결과가 통지된느 경우가 있으므로 수정했습니다.

## 버전 0.45.0(2020/1/15)

### 기능 추가

- 「테스트 일괄 실행」 설정을 브라우저상이 아닌 서버상에 보존 할 수 있습니다.
  - 다른 PC에서 로그인하거나 브라우저 캐시삭제를 해도 설정이 초기화 되지 않습니다.
- 「테스트 일괄 실행」 설정을 프로젝트 전체에서 공유 할 수 있습니다.
- 「테스트 일괄 실행」 설정에서 「외부 클라우드 서비스」의 「액세스 키」「액세스 토큰」「TestObject API 키」값을 보존 할 수 있습니다.
  - 암호화하여 저장됩니다.
- 「테스트 일괄 실행」 설정에서 「클라우드」 환경에 대해서도 「디바이스 패턴 추가」가 가능합니다.
- [magic-pod-api-client]로부터 복수의 디바이스 패턴을 사용한 일괄 실행이 가능합니다.
  - [magic-pod-api-client]의 [버전 0.45.0.1]을 설치해야 합니다.
- cross-batch-run의 Web API의 반환값으로서 각종 테스트 정보가 되돌아옵니다.
- iOS12 시뮬레이터에서도 「WebView 요소를 HTML로 스캔」 설정을 이용할 수 있습니다.
- 「클라우드」환경에서도 Android의 「WebView 요소를 HTML로 스캔」설정을 이용 할 수 있습니다.
- 신규 작성된 테스트의 테스트 대상 설정의 디폴트 값으로 전단계 테스트의 테스트 대상 설정 값을 이용합니다.
- app 파일을 포함하지 않는 zip 파일을 테스트 대상 앱으로서 클라우드 업로드를 하면 에러가 발생하도록 설정했습니다.
- iOS 클라우드 디바이스의 샘플 앱을 UI 카탈로그 데모 앱에서 훨씬 더 안정적으로 사용 할 수 있는 VodQA 샘플 앱으로 변경했습니다.
- 클라우드 디바이스 기동/접속 시 동작을 개선했습니다.
- 테스트 결과 페이지의 세세한 표시를 개선했습니다.
- 첫 회 등록 페이지의 세세한 표시를 개선했습니다.

### 오류 수정

- 테스트 편집 페이지에서 다른 페이지로 이동할 경우, 클라우드 디바이스의 화면 캡쳐 취득에 실패하는 경우가 있어 수정했습니다.
- 프로젝트를 복제 했을 때 「디바이스에 이미지를 추가」 명령어의 이미지가 복제되지 않는 오류를 수정했습니다.

## 버전 0.44.0(2019/12/22)

### 기능 추가

- Magic Pod가 제공하는 Android 클라우드 에뮬레이터를 사용해 테스트 작성 및 실행이 가능합니다.
- iOS의 테스트 작성시에 테스트 대상 지정 패널로 「WebView 요소를 HTML로 스캔」을 지정할 수 있습니다.
  - HTML로 스캔한 화면 캡쳐는 현재 iOS13 시뮬레이터 테스트만 이용 가능합니다.
- iOS 클라우드 디바이스의 Web API 실행으로 iPhone 8 이외의 디바이스도 지정 가능합니다.
- 로컬 PC의 iOS 시뮬레이터에서 Xcode11.3, iOS13.3의 이용이 가능합니다.
- 로컬 PC의 iOS 시뮬레이터 기종에서 버전에 대응하는 것만 표시되도록 설정되어있습니다.

### 오류 수정

- 사내 프록시 환경에서 Magic Pod Desktop이 제대로 작동하지 않는 오류를 수정하였습니다.
- 복사된 명령어를 포함한 테스트가 있는 프로젝트를 복제하면 에러가 발생하는 경우가 있어서 수정했습니다.
- Web API model parameter를 지정하지 않은 경우에 서버 에러가 발생할 수 있어 수정했습니다.
- 별도의 프로젝트에 클라우드 디바이스를 변경할 시, 이전 프로젝트 세션이 남아있으면 변경이 불가능한 오류를 수정했습니다.
- 브라우저 테스트에서 발생한 각종 오류를 수정했습니다.
  - magic_pod_config.json에서 chromeOptions을 지정하면 에러가 발생하여 수정했습니다.
  - 일부 UI요소의 로케이터가 생성 되지 않고 테스트 실행 및 편집에서 에러가 발생하여 수정했습니다.

## 버전 0.43.0(2019/12/8)

### 기능 추가

- Android 테스트 작성시에 테스트 대상 지정 패널에서 「WebView의 새로운 스캔을 유효화」를 지정하면, WebView를 HTML로 스캔 가능합니다.
  - HTML로 스캔한 화면 캡쳐는 현재 로컬 PC 테스트만 이용 가능합니다.
- 테스트 실행시 로그 레벨을 테스트 대상 지정 패널 및 Web API의 인수로 지정 할 수 있습니다.
- 첫 회 등록 과정에서 불편함이 발생했던 부분들을 개선했습니다.
- 브라우저 테스트 부분을 개선했습니다.
  - Linux상에서 명령 라인 일괄 실행이 가능합니다.
  - 테스트 실행시 화면 캡쳐의 캡쳐 범위를 HTML만 지정할 것인지 윈도우 전체를 지정할 것인지 선택 할 수 있습니다.
  - URL 지정 다이얼로그의 움직임을 개선했습니다.

### 오류 수정

- 「이미지를 추가」명령어 편집중에 에러가 발생하는 경우가 있어 수정했습니다.
- 네트워크 에러의 영향으로 디바이스 부팅에 실패하는 경우가 있어 수정했습니다.
- GitHub 계정을 연동하여 유저 등록을 진행하는 과정에서 오류가 발생하여 수정했습니다.

## 버전 0.42.0(2019/11/24)

### 기능 추가

- 요금 체계를 변경했습니다.
  - 클라우드 디바이스를 시간제한 없이 이용 가능한 [「스탠다드」플랜]의 제공을 개시 했습니다.
  - 「베이직」「어드벤스」요금제는 신규 접수 종료되었습니다. (기존 「베이직」「어드벤스」사용자는 제외)
  - 향후 신규 등록한 조직에서는 「외부 클라우드 서비스」 제휴는 유료 플랜으로만 제공됩니다.
- 신규 사용자 등록시 입력항목 및 등록 진행 방법을 변경했습니다.
- 클라우드 업로드 된 파일을 리스트업 및 삭제하는 Web API를 추가했습니다.
- 테스트 일괄 실행 Web API에서 「app_file_number」에 「latest」를 지정하면 항상 최신의 클라우드 업로드된 파일을 지정할 수 있습니다.
- 결제 정보 입력 페이지의 UI를 변경했습니다.
- 「테스트」「공유 스텝」의 「정보」페이지에서 「삭제」 버튼을 삭제했습니다. (「조직」「프로젝트」의 삭제버튼을 혼동하여 클릭하지 마시기 바랍니다.)
- 「조직」「프로젝트」삭제시 뜨는 삭제 확인 메시지를 보다 강하게 인식할 수 있도록 변경하였습니다.
- 로컬 PC에서「테스트 실행」「화면 캡쳐 취득」「앱 정보 취득」진행 시에 브라우저 확인 다이얼로그가 표시 되지 않습니다.
- 브라우저 테스트이 대해 여러가지를 개선했습니다.
  - 경고가 존재하지 않는 경우,「경고 메시지가 일치하는 경우」「경고 메시지가 포함된 경우」명령어가 에러가 아닌 단순이 조건이 충족되지 않은 것 입니다.

### 오류 수정

- Android 클라우드 디바이스의 기동 실패하는 경우가 있어 해당 오류를 수정했습니다.
- 시크릿 변수 값이 너무 짧을 경우, 에러 메시지가 올바르게 표시 되지 않는 오류를 수정했습니다.
- 브라우저 테스트의 여러 오류들을 수정했습니다.
  - 줄 바꿈 텍스트에 대한 로케이터가 올바르게 생성되지 않는 오류를 수정했습니다.
  - WindowsのChrome에서 「다운로드 된 것 체크」「다운로드가 완료 될 때까지 대기」명령어가 실패하는 경우가 있어서 수정했습니다.

## 버전 0.41.0(2019/11/10)

### 기능 추가

- Web API를 보다 간편하게 명령 라인에서 실행 가능하도록 [magic-pod-api-client]를 준비했습니다.
- 일괄 테스트 실행 화면에서 magic-pod-api-client 형식으로 설정을 복사할 수 있습니다.
- 로컬 PC의 iOS 시뮬레이터에서도 「디바이스 언어」「디바이스 지역」을 설정 할 수 있습니다.
- 로컬 PC의 iOS 시뮬레이터에서 Xcode11.2, iOS13.2를 이용 할 수 있습니다.
- 브라우저 테스트 부분들을 개선했습니다.
  - 「경고 메시지」가 「일치하는지」「포함하는지」에 대한 「체크」「대기」「케이스」명령어를 추가했습니다.
  - 「경고 메시지를 저장」 명령어를 추가했습니다.
  - 일부 부정확한 형식의 HTML 내부 텍스트를 로케이터에 이용할 수 있도록 수정했습니다.

### 오류 수정

- 클라우드 디바이스를 마우스 조작을 수동으로 해도 반응하지 않는 오류를 수정했습니다.
- Mac CatalinaでMagic Pod Desktop를 처음 다운로드 했을 때, 제대로 열리지 않는 문제를 수정했습니다.
- Android 클라우드 디바이스의 일괄 테스트 실행이 실패하는 경우가 있어 해당 오류를 수정했습니다.
- 「디바이스에 이미지를 추가」명령어를 포함한 테스트를 개시할때, 에러가 발생하는 경우가 있어 해당 오류를 수정했습니다.
- 횟수 및 초(秒/second)수 등의 수치를 취하는 값이 공유 스텝의 parameter가 되면 테스트 실행에 실패하기 때문에 해당 내용들을 parameter로 할 수 없도록 조치했습니다.
- Web API의 shared_data_pattern parameter의 형식이 부정확한 경우에 서버 에러가 발생하는 오류를 수정했습니다.
- 프로젝트 삭제에 실패하는 경우가 있어 해당 오류를 수정했습니다.
- 브라우저 테스트 관련하여 각종 오류를 수정했습니다.

## 버전 0.40.0(2019/10/27)

### 기능 추가

- 여러 테스트의 일괄실행 설정을 작성 할 수 있습니다.
- 외부 클라우드 서비스의 일괄 테스트 실행 설정에 대해 화면에서 복수의 「디바이스 패턴」을 추가 할 수 있습니다.
- Magic Pod가 제공하는 Android 클라우드 에뮬레이터를 사용하여, 테스트 일괄실행이 가능합니다. (유료 플랜만)
- SauceLabs의 iOS/Android 테스트, BrowserStack의 Android 테스트에서 사용하는 Appium 버전이 [1.15.0]로 변경 되었습니다.
- 테스트 편집화면의 세세한 부분들을 개선했습니다.

### 오류 수정

- Web API에 전달된 클라우드 디바이스의 「version」「model」이 실제 값과 다른 경우, 전달된 값이 테스트 결과 화면에 표시되는 오류를 수정했습니다.

## 버전 0.39.0(2019/10/19)

### 오류 수정

- iOS13 또는 Xcode11의 화면 해석 및 테스트 실행시에 존재해야 할 요소를 찾을 수 없는 경우가 있어 해당 오류를 수정했습니다.
- Web API에서 테스트 실행 parameter가 부정확한 경우에 시스템 에러가 발생하는 경우가 있어 수정했습니다.

## 버전 0.38.0(2019/10/13)

### 기능 추가

- 「디바이스에 이미지를 추가」 명령어를 추가했습니다. (iOS 실제 기기는 미대응)
- 권한 관리 기능의 이용 여부를 조직 관리 화면에서 선택 할 수 있습니다. (유료 플랜만 가능)
- 조작 실수 방지를 위해, 권한 관리가 유효화 된 경우에는 「조직 사용자 추가시의 전체 프로젝트 일괄 추가」「프로젝트 작성시의 기존 사용자 전체 추가」기능이 무효화 됩니다.
- 클라우드 디바이스의 iOS버전을 13.1로, Appium 버전을 [1.15.0]으로 변경했습니다.
- 외부 클라우드 서비스 복수 패턴 일괄 실행 결과 메일이 하나로 정리되어 송부되어집니다.
- 테스트 결과 메일 서식을 보기 쉽게 개선했습니다.
- iOS의 「스위치 조작」「picker 값 입력」 명령어의 안정성을 강화했습니다.

### 오류 수정

- 프로젝트 소유자에게 테스트 결과 메일을 보낼 수 없는 경우가 있어 해당 오류를 수정했습니다.
- 공유 스텝을 작성 할 수 없는 경우가 있어 해당 오류를 수정했습니다.
- iOS의 UI트리 스캔에 실패하는 케이스가 있어서 수정했습니다.

## 버전 0.37.0(2019/10/2)

### 기본 문제와 해결책

Xcode11에서 iOS12 이전 시뮬레이터/실제 기기에서 「로컬 PC」테스트를 진행하면 「초기 표시 텍스트가 있는 텍스트 입력란」에 대해 이하의 문제가 발생합니다.

- 正しいテキストの値が取得できない。([原因](https://github.com/appium/appium/issues/13302))
- テキスト入力の際に、既存のテキストがクリアされず追記されてしまう。([原因](https://github.com/appium/appium/issues/13288))

こちらはXcodeの問題で現状有効な回避策がありません。問題を回避したい場合は、iOS12以前のテストにはXcode10.3を利用してください。少し面倒ですが、[こちら](https://www.trident-qa.com/magic-pod-frequent-troubles/#switch_multi_xcodes)の手順に従えば、複数バージョンのXcodeをPCにインストールして切り替えて使うことも可能です。

### 機能追加

- 利用するAppiumのバージョンを[1.15.0](https://github.com/appium/appium/releases/tag/v1.15.0)にしました。
  - Xcode11、iOS13、iPhone11を使ったローカルPCテストが可能になりました。

## バージョン0.36.0(2019/9/28)

### 機能追加

- テスト一括実行結果画面から、テスト実行中に「画面キャプチャ」コマンドで取得した画像だけをダウンロードできるようになりました。
- テスト一括実行結果画面キャプチャを、Web APIでダウンロードできるようになりました。([#114](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/114))
- SauceLabsを使ったテスト実行の際に、利用するデータセンターを指定できるようになりました。
- 外部クラウド一括実行の開始時に発生したエラーの内容を、テスト結果画面で確認できるようになりました。
- 「アプリURL」指定でBrowserStackのテストを実行する場合に、URL末尾が.ipaや.apkで終わらなくても実行できるようにしました。
- テスト一括実行画面で取得できるcurlコマンドが、HTTPレスポンスもチェックするようにしました。
- テスト一括実行結果画面でダウンロードできる画面キャプチャファイルの内容の細かい改善を行いました。
- 「タップ」コマンドの安定性を強化しました。
- 各種画面のUIの細かい改善を行いました。
- ブラウザテスト周りの各種改善を行いました。(特定ユーザーのみ)
  - Internet Explorerでのテスト実行が安定してできるようになりました。
  - MagicPodDesktop.exeでもバージョンアップ通知を受け取れるようになりました。
  - テスト失敗時に、HTML以外の部分(URLバー、ポップアップなど)を含む画面キャプチャを見られるようになりました。

### 不具合修正

- 共有ステップを含むテストのログ中の行番号がずれることがある不具合を修正しました。
- iOSのUIツリーのスキャンに失敗するケースがあったので修正しました。

## バージョン0.35.0(2019/9/14)

### 機能追加

- [SauceLabs](https://saucelabs.com/)を使った一括テスト実行が可能になりました。
- 「(アプリ情報をクリア)」のアプリ起動オプションを、クラウド端末およびRemote TestKitでも利用できるようになりました。
- Log Levelに関わらず、Appiumコンソールの内容が常にテスト結果画面に表示されるようにしました。
- ブラウザテスト周りの各種改善を行いました。(特定ユーザーのみ)
  - Internet Explorerでのテスト実行処理の安定化を行いました。
  - テスト編集画面のズーム比率・画面分割位置の挙動を改善しました。

### 不具合修正

- 「表示されるまでスワイプ」が、iOSのWebView上の要素に対しうまく動作しないケースがあったので修正しました。
- 共有ステップ編集画面で「元に戻す」「やり直す」の処理がうまくいかなくなっていたので修正しました。

## バージョン0.34.0(2019/8/31)

### 機能追加

- 独自定義したロケータの値内で${1}のように書くことで、コマンドからロケータのパラメータを簡単に指定できるようになりました。
- [BrowserStack](https://www.browserstack.com/)を使った一括テスト実行が可能になりました。
- HeadSpinを使った一括テスト実行がWeb APIからも可能になりました。
- 現在開発中のAndroidの「新バージョンの実行エンジン」を、テスト対象指定パネル上で選べるようになりました。
  - Espresso DriverとUiAutomator2 Driverを併用しています。
  - 現在ローカルPCでのテスト実行時のみ有効です。
- テスト実行の中断がより高速に行われるようにしました。
- クラウド端末でのテスト実行中に手動の端末操作が無効になっていたので、有効にしました。
- テスト対象指定パネルの一部のパラメータからシークレット変数を参照できるようになりました。
- ブラウザテスト周りの各種改善を行いました。(特定ユーザーのみ)
- データ読み込みが遅い、いくつかのページの処理速度を改善しました。

### 不具合修正

- 共有ステップのパラメータにUI要素を指定した場合に、そのパラメータを参照しているコマンドの他のパラメータが不正になることがあったので修正しました。
- iOSの画像解析が失敗するケースがあったので修正しました。

## バージョン0.33.0(2019/8/18)

### 機能追加

- 共有ステップの変数にUI要素を指定できるようになりました。
- iOSの要素ツリー取得ロジックを高速化しました。
- テスト結果画面のAppiumログの内容を改善しました。
- Magic Podデモアプリのデータ登録後の画面に「戻る」ボタンを追加しました。
- クラウド端末のiOSシミュレータのバージョンが12.2になりました。

### 不具合修正

- 1コマンドだけの共有ステップが作成できない不具合を修正しました。
- Androidのテスト実行後に端末のIMEを元に戻す処理がうまくいかなくなっていたので修正しました。

## バージョン0.32.0(2019/8/3)

### 機能追加

- クラウド端末のパフォーマンスおよび安定性を抜本的に改善しました。
- 一致するか/含むかのアサート/待機/条件分岐のコマンドの値に、複数行テキストを指定できるようになりました。([#109](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/109))
- UI要素のロケータを削除できるようになりました。(ユーザーが追加したロケータに限ります)
- テスト失敗時画面の解析を非同期にして、テスト失敗時のテスト終了までのパフォーマンスを改善しました。
- クラウド端末のテスト対象アプリに「UIカタログデモアプリ」を利用できるようになりました。 
- 組織名とプロジェクト名に「-」「_」「.」が利用できるようになりました。
- ローカルPCのiOSシミュレータのバージョンに12.3と12.4を指定可能になりました。
- テキスト方向に関する制御文字が要素のテキストに含まれていた場合、アサート/待機/条件分岐等のコマンドでは無視するようにしました。
- Magic Pod Desktop上のボタンは、押すと誤動作につながるケースが多いため削除しました。
- テスト編集画面の細かい修正を行いました。
- クラウド端末関連の細かい修正を行いました。

### 不具合修正

- 一部のコマンドの値内で変数参照が値に変換されない不具合を修正しました。
- 存在しないUI(削除されたプロジェクトのUIなど)を含むテストを実行するとサーバエラーになる不具合を修正しました。
- 共有でないUIが共有ステップに使えるケースがあったので修正しました。
- テスト編集画面で共有ステップを利用しているコマンド行において、共有ステップ内部の行をクリックしても対応するUI要素がハイライトされなくなっていたので修正しました。

## バージョン0.31.0(2019/7/21)

### 機能追加

- テスト編集ページの右クリックメニューまたはキーボードショートカットで、コマンドのコピー・切り取り・貼り付け・削除が可能になりました。([#108](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/108))
- テスト編集ページで、編集内容の取り消し/やり直しが可能になりました。
- テスト編集ページで、十字キーで現在の選択コマンドの切り替えが可能になりました。
- 「スイッチ操作」のコマンドを追加しました。
- 「メンバー参照」権限を設定可能になりました。(有料プランのみ)
- 新規プロジェクト作成時の「メンバー全員をプロジェクトに追加する」のチェックを、デフォルトでオフにしました。(有料プランのみ)
- 利用するAppiumのバージョンを[1.14.0](https://github.com/appium/appium/releases/tag/v1.14.0)にしました。
  - 一部のバージョンのAndroid端末で画面キャプチャが取得できない不具合などが解消されています。([#112](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/112))
- 失敗テストに対するテスト結果画面の読み込み速度を改善しました。
- ファイルのクラウドアップロード速度と、アップロードしたファイルのテスト実行時のダウンロード速度を改善しました。
- 「認証情報」の設定名を「シークレット変数」に変更しました。
- ブラウザテスト周りの各種改善・機能追加・不具合修正を行いました。(特定ユーザーのみ)

### 不具合修正

- UIの上書きアップロードに失敗するケースがあったので修正しました。
- 「フリック」コマンドを含むテスト結果画面でエラーが出るケースがあったので修正しました。
- 待ち処理系のコマンドの最大待ち時間を空にするとテスト実行時にエラーになる不具合を修正しました。
- テストケース数が組織ごとの上限数よりも1つ多く作れるようになっていたので修正しました。
- エンタープライズ・商用サポートプランを利用している場合にも、設定ページから他のプランを申し込めるようになっていたので修正しました。

## バージョン0.30.0(2019/7/7)

### 下位互換性のない機能追加

- Magic Pod Desktopを使った[コマンドライン一括実行](https://www.trident-qa.com/magic-pod-batch-test-run/#sec2)をしている場合、内部仕様の変更に伴い「Signature has expired」というエラーが出るようになるので、Magic Pod Desktopに再度ログインしてください。
- 古いバージョンのUI解析ロジックで計算されたAIロケータを使ってテスト実行する場合にも、新しいUI解析ロジックを使用するようにしました。
- Androidのテスト実行ロジックを、「新バージョンの実行エンジン」(UIAutomator2)に完全に移行しました。

### 機能追加

- テスト設定ポップアップおよびWeb APIで、「認証情報」を指定できるようになりました。([#53](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/53))
  - ログ上でマスクしたい情報を指定するのに使えます。
- テスト一括実行のテスト設定ポップアップおよびWeb APIで、テストケース番号を指定できるようになりました。
- 安定化のために、ローカルPCテスト時のiOS端末への接続ロジックを改善しました。
  - これに伴い、Magic Pod Desktopでアプリを指定せずに端末を起動すると、「設定」アプリが初期アプリとして起動されるようになります。
- iOSの「-ios class chain」のロケータを、「xpath」よりも優先的に使用するようにしました。
  - 要素をより高速に見つけられるようになります。
- テスト実行時のタイムアウトを、クラウドの場合は20分、ローカルPCの場合は40分に延長しました。
- テスト失敗時の画面キャプチャ・画面ツリーのうち片方の取得に失敗しても、もう片方はアップロードするようにしました。
- テスト編集画面の細かい改善を行いました。
- 一括テスト実行結果一覧画面のパフォーマンス改善を行いました。
- ブラウザテスト周りの各種改善・機能追加・不具合修正を行いました。(特定ユーザーのみ)

### 不具合修正

- Remote TestKit Enterpriseでクラウドアップロードされたアプリのテスト実行が失敗する不具合を修正しました。
- クラウドアップロードするアプリのファイル名に半角英数字記号以外が含まれていた場合に、分かりやすいエラーメッセージを出すようにしました。
- 動作しない古いバージョンのシミュレータがテスト対象として選択可能になっていたので削除しました。
- アイコンに対するAIロケータのテスト実行が失敗するようになっていたので修正しました。


## バージョン0.29.0(2019/6/23)

### 機能追加

- テスト一括実行結果のページからテスト結果の画面キャプチャをダウンロードできるようになりました。([#105](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/105))
- テスト結果画面の「画面キャプチャ一覧」を拡大・縮小機能できるようになりました。
- Remote TestKit (On-premise含む)のテストを、複数のバージョン・機種の組み合わせで一括実行できるようになりました。
  - バージョン・機種を「,」区切りで指定してください。
- テスト編集画面でUI画像の表示サイズを変更できるようになりました。
- Remote TestKitのAndroidテスト実行で利用するAppiumのバージョンを[1.12.1](https://github.com/appium/appium/releases/tag/v1.12.1)にしました。
- テスト対象指定パネルの細かい改善を行いました。
- ブラウザテスト周りの各種改善を行いました。(特定ユーザーのみ)

## バージョン0.28.0(2019/6/9)

### 機能追加

- テストの一括実行結果一覧ページに、テストを実行した端末名・iOSバージョンなどのラベルが表示されるようになりました。
- 「スワイプ」コマンドのパラメータで「圧力」を指定できるようになりました。([#93](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/93))
- Remote TestKitのテスト結果ページでAppiumコンソールの出力内容を確認できるようになりました。
  - Androidのみ対応しています。(iOSのAppiumコンソール出力の取得は、Remote TestKit側が未対応のため)
- テスト編集画面のUIの細かい改善を行いました。 ([#107](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/107), [#96](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/96))
- UI解析のロジックを改善し、要素の重なりの順序をより正しく判定できるようになりました。
- 安定化のために、ローカルPCテスト時のAndroid端末への接続ロジックを改善しました。
- Magic Pod Desktopの「環境チェック」の機能はメンテナンスされておらず、かつ通常の端末接続機能と同じことしかしておらず有用性がないため、削除しました。
- ブラウザテスト周りのインストーラ・UI等の改善を行いました。(特定ユーザーのみ)

### 不具合修正

- テスト結果のAppiumログが長すぎるとテスト結果画面でエラーになっていたので、長すぎるログは中間を省いて保存するようにしました。
- テスト失敗時に画面キャプチャの解析が失敗するとテストがいつまでも終了しない不具合を修正しました。
- 作成した共有ステップが不正になるケースがあったので修正しました。
- 「外部クラウドサービス」でテスト実行ができなくなっていた不具合を修正しました。

## バージョン0.27.1(2019/5/30)

### 機能追加

- デスクトップブラウザのテストができるようになりました。現在は特定ユーザーのみのクローズド利用となっています。

## バージョン0.27.0(2019/5/26)

### 機能追加

- [料金プラン](https://www.trident-qa.com/magic-pod-plan/)を改定しました。
  - 有料プランのテストケース数は、50テストケースと100テストケースの2段階になりました。
  - 有料プランにするとクラウド端末の利用時間がプラスされるようになりました。
  - すでに有料・商用サポートプランを契約済みの方にはプラン内容・支払い内容に変更はありません。
- 組織の設定画面からクレジットカードで有料プランを購入・キャンセル可能になりました。
- OS・端末などのテスト実行時情報がテスト結果画面で確認できるようになりました。
- セキュリティ強化のため、ユーザー登録時のメールアドレス認証が必須になりました。([#71](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/71))
  - 未認証のアカウントの場合、次回ログイン時に認証を求められます。
- メールアドレスを指定して組織・プロジェクトのメンバーを追加できるようになりました。([#102](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/102))
- Appファイルを指定した場合のクラウド端末起動時間を改善しました。
  - アプリを端末にダウンロードする時間が約1/2になっています。
- テスト実行時の要素の検索処理の速度を改善しました。
- クラウド端末でもUIの再アップロードができるようになりました。
- Magic Pod Desktopの「Log Level」を「Expert」以上にしてテスト実行すると、テスト実行時のAppium Logが常にテスト結果画面で確認できるようになりました。
- バージョン0.26.0の変更(「Remote TestKit iOSテスト」の追加、「端末の地域」の追加、「端末の言語」の選択肢の追加)が、Bitrise Step側からも利用可能になりました。
  - Bitrise Stepの指定バージョンを0.26.0以上にする必要があります。

### 不具合修正

- クラウドのテスト一括実行の合計時間が10分間を過ぎると、一括実行が強制終了される不具合を修正しました。
- テスト結果の画面キャプチャ・UIツリーなどのサイズが大きいと、テスト終了時にエラーが発生してしまう不具合を修正しました。
- Androidにおいて、タップすると画面遷移が発生する要素をダブルタップするとエラーになる不具合を修正しました。

## バージョン0.26.0(2019/5/10)

### 機能追加

- UIの整理を容易にするため、UI一覧画面にて、他のテストと共有するUIとしないUIを分けられるようにしました。
  - 今後新たに登録したUIは、初期状態では他のテストと共有されないようになっています。
  - 他のテストと共有しないUIは、テストを削除すると同時に削除されるので注意してください。
- データパターン設定(データ駆動テスト定義)のCSVアップロード/ダウンロードが可能になりました。
- Remote TestKitを使ったiOS実機の一括テスト実行が可能になりました。(Bitrise Step側は後日対応します)
- テスト編集画面にて、「端末の地域」を指定可能になりました。(Bitrise Step側は後日対応します)([#98](https://github.com/Magic-Pod/japanese-issue-and-doc/issues/98))
- テスト編集画面にて、「端末の言語」に「デフォルト」「韓国語」を指定可能になりました。(Bitrise Step側は後日対応します)
- テスト編集画面のUIの細かい改善を行いました。

### 不具合修正

- テスト編集画面のUI一覧ポップアップの細かい不具合を修正しました。
- テスト数・UI数が多いとテストの保存処理が重くなっていたので修正しました。
- 複数タブを開いているとクラウド端末の起動が正しくできなくなるケースがあったので修正しました。

## バージョン0.25.1(2019/4/26)

Magic Pod Desktopの変更はありません。

### 機能追加

- 組織のアイコンを設定できるようになりました。
- オンプレミス版のRemote TestKitを使った一括テスト実行が可能になりました。
- Remote TestKitテスト実行で利用するAppiumのバージョンを[1.10.1](https://github.com/appium/appium/releases/tag/v1.10.1)にしました。
- テスト編集画面のUI画像上で、要素を一時的に隠す機能を実装しました。
  - 他の要素の後ろに隠れてしまっている要素を選択したい際に使えます。
  - 今後新たにスキャンした画像に対してのみ有効です。
- UIまわりの各種改善を行いました。

### 不具合修正

- テスト実行時に取得する画面キャプチャ数が多いと、テスト結果をうまく保存できないことがあったので修正しました。
- Magic Pod Desktopのコマンドラインからのupdateができなくなっていたので修正しました。

## バージョン0.25.0(2019/4/23)

### 機能追加

- UI解析のロジックを刷新しました。要素のシステム情報を積極的に活用しています。
  - 従来に比べ数分の1の時間で解析が完了します。 
  - 従来に比べ領域をはるかに正確に検出できます。
- クラウド端末が、PCキーボードからのキー入力を受け付けるようになりました。(日本語は未対応です)
- クラウド端末のテスト結果画面で、テスト失敗時のAppiumコンソールの内容が確認できるようになりました。
- iOS12.2に対応しました。(ローカルPC実行)
- 利用するAppiumのバージョンを[1.12.1](https://github.com/appium/appium/releases/tag/v1.12.1)にしました。
- UIまわりの各種改善を行いました。

### 不具合修正

- Carthageが無いとiOSのPCローカルテストの作成・実行ができない不具合を修正しました。
- クラウド端末に対するスワイプ操作の終点が画面外だとスワイプが反応しない不具合を修正しました。
- UI画像が縦に長すぎるとテスト編集画面の表示が崩れる不具合を修正しました。
- クラウド端末の動作が不安定になる各種不具合を修正しました。

## バージョン0.24.5(2019/3/26)

Magic Pod Desktopの変更はありません。

### 機能追加

- Bitriseからテスト一括実行を呼び出せる[Magic Pod UI test](https://blog.trident-qa.com/2019/03/bitrise-magic-pod/)のステップを作成しました。
- 作成したテストを[Headspin](https://www.headspin.io/)の「外部クラウドテストサービス」で実行できるようになりました。
- クラウド及び外部クラウドサービスのテスト対象のappファイルとして、zipファイルをアップロードできるようになりました。
- テスト結果画面の「画面キャプチャ一覧」の表示を高速化しました。
- テスト編集画面のUI要素情報の表示を改善しました。

## バージョン0.24.4(2019/3/18)

Magic Pod Desktopの変更はありません。

### 機能追加

- 「すべてのステップ」のテスト実行キャプチャが全てのユーザーで利用可能になり、これがデフォルトになりました。
- テスト結果画面のデフォルトタブを「キャプチャ一覧」にしました。
- Magic Podからのメールが素早く配信されるようになりました。

## バージョン0.24.3(2019/3/8)

Magic Pod Desktopの変更はありません。

### 機能追加

- 一括テスト実行のテスト対象設定を、curl/Invoke-RestMethodコマンドの形式で取得できるようになりました。
- 一括テスト実行のメール配信設定と失敗時のリトライ回数の設定がブラウザに保存されるようになりました。
- Remote TestKitのAPKファイル/URL指定のテスト実行の場合に、Appパッケージ/Appアクティビティの値指定が不要になりました。
- 一括テスト実行Web APIの細かい改善を行いました。

### 不具合修正

- 一括テスト実行Web APIの各種不具合修正を行いました。

## バージョン0.24.2(2019/3/6)

Magic Pod Desktopの変更はありません。

### 機能追加

- Web APIからクラウドおよび外部クラウドサービスに対するテスト実行が可能になりました。
  - ユーザーメニューのAPIトークンのページから、トークンの生成およびAPIの仕様確認が行えます。
- クラウド端末への初回アクセスの際に、手順を解説する動画を表示するようにしました。

### 不具合修正

- クラウド端末の画面キャプチャやテスト結果ログが常に英語になっている不具合を修正しました。
- クラウド端末/外部クラウドサービスのテスト一括実行が開始前に異常終了すると、テストが実行中のステータスのままになってしまうことがある不具合を修正しました。
- ブラウザの言語が日本語の場合にUIのセクション分割が行えなくなっていた不具合を修正しました。

## バージョン0.24.1(2019/2/27)

Magic Pod Desktopの変更はありません。

### 機能追加

- クラウド端末のテスト対象アプリとして「初期アプリ」「Magic Podデモアプリ」を選択できるようになりました。

### 不具合修正

- テスト結果画面のテスト行の表示において、共有ステップのパラメータの値が空になっている不具合を修正しました。

## バージョン0.24.0(2019/2/24)

Magic Pod Desktopの変更はありません。

### 機能追加

- テスト対象環境のデフォルトを「ローカルPC」から「クラウド」に変更しました。
- テストの一括実行がクラウドのシミュレータで利用可能になりました。
- 共有ステップの中身がテスト結果画面で確認できるようになりました。
- 共有ステップの中身も現在実行中のテスト行のカーソル表示とMagic Pod Desktopの行表示でうまく扱えるようにしました。
- テスト編集画面のテスト対象指定パネルを2タブに分割し、さらに文言をわかりやすく改善しました。
- クラウド環境のiOSシミュレータのテストに対しても「端末の言語」を指定できるようになりました。
- テスト結果画面に条件分岐コマンドの中のコマンドは字下げして表示するようにしました。
- ヘルプページ等を除く製品の全ての表示を完全に英語対応しました。

### 不具合修正

- クラウドシミュレータに関する各種不具合を修正しました。
- テストの部分実行ができなくなっていたので修正しました。
- Remote TestKitでApkファイルを使ったテスト実行ができなくなっていたので修正しました。

## バージョン0.23.0(2019/2/13)

### 下位互換性のない機能追加

- iOSシミュレータのテストの際に言語を強制的に日本語に変更していたのを、シミュレータの現在の言語をそのまま利用するように変えました。

### 機能追加

- Magic Podのクラウド環境にあるiOSシミュレータでテストを作成・実行できるようになりました。
- テスト編集画面で「デバイスの言語」を指定できるようになりました。(ただしiOSシミュレータでは未対応です)

## バージョン0.22.0(2019/2/12)

### 下位互換性のない機能追加

- Magic Pod Desktopを使ったコマンドライン実行が異常終了した場合の返り値を、1から-1に変更しました。
- iOS実機のテストの際に言語を強制的に日本語に変更していたのを、端末の言語をそのまま利用するように変えました。

### 機能追加

- テスト実行およびテスト一括実行を、画面から中断できるようになりました。
- テスト実行中に、現在何行目を実行しているかをテスト編集画面で確認できるようになりました。
- テスト編集画面の各UI要素の編集ポップアップにて、新しいロケータ定義を追加できるようになりました。
- テスト編集画面のUI一覧のセクションの開閉状態を保存できるようにしました。
- テスト編集画面のUI一覧の読み込み速度を改善しました。
- Androidのresource-id/content-desc/text、iOSのname/labelが、同じ値のものが複数あった場合にも、これをできるだけロケータに利用するようにしました。
- iOS12.1に対応しました。
- 利用するAppiumのバージョンを[1.10.0](https://github.com/appium/appium/releases/tag/v1.10.0)にしました。
- UIの上書きアップロードで、古い要素の現在のロケータが新しいUI上に無い場合でも、より柔軟にUI情報を更新できるようにしました。
- UIスキャン・UI再アップロード・テスト実行などの通知が同じプロジェクト内の別ユーザーにはいかないようにしました。
- プロジェクト単位でのメンバーの追加・削除が可能になりました。(商用サポートユーザーのみ)
- メンバーの組織・プロジェクトの更新・プロジェクト作成・テストケース更新・削除の権限を変更可能になりました。(商用サポートユーザーのみ)

### 不具合修正

- Androidのidロケータを、常に現在のアプリ名を含まない形式で生成するとうまく動作しないことがあったので、現在のアプリ名を表す${APP_PACKAGE}という特殊な変数を含む記法に変更しました。
- 初回登録の画面で組織を空にするとサーバエラーになる不具合を修正しました。
- テスト0行目の「アプリを起動」の行だけを選択してテストを部分実行できない不具合を修正しました。
- iOSのコマンド実行に時間がかかりすぎるとUnknownErrorになる不具合を修正しました。
- 対象のUI要素が無いコマンドを実行するとサーバエラーになることがある不具合を修正しました。
- プロジェクトを削除する際に、サーバエラーになることがある不具合を修正しました。
- テスト結果の一覧画面にて、サーバエラーになることがある不具合を修正しました。
- テスト失敗時の端末のUIツリーが不正な形式だった場合に、テスト結果画面にうまく表示されないケースがあったので修正しました。
- UIの数が多いと共有ステップの保存でサーバエラーになることがある不具合を修正しました。
- コマンドの入力文字列が空だったり長すぎるとテスト結果画面のレイアウトが崩れる不具合を修正しました。
- Remote TestKitテスト実行用のAPKファイルをアップロードした状態でプロジェクトを複製するとエラーになる不具合を修正しました。(商用サポートユーザーのみ)
- 多言語定義の取り込みで定義ファイルにエラーがあるとデータ不整合になる不具合を修正しました。(商用サポートユーザーのみ)

[もっと古いCHANGELOGを見る](https://github.com/Magic-Pod/japanese-issue-and-doc/blob/master/CHANGELOG_OLD.md)
